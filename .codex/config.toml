# Shared MCPs such as github/context7/sentry are configured globally in ~/.codex/config.toml.
# The Playwright MCP is temporarily disabled here as well.
# Current Codex desktop build treats `resources/list` / `resources/templates/list`
# failures from `@playwright/mcp` as fatal on this machine.
# Repo-local playwright stays reserved for this project's own tests and scripts.
