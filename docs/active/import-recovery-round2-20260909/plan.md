# Round 2 implementation

## Goal and source

Complete the second response of https://chatgpt.com/c/6aa0b8ff-8bec-83ec-a70c-f01773c7b088 against main at 75ccc19e.

## Scope and acceptance

1. Required import recovery has a restricted failure state; retry completes dependent indexes and UI rendering. Optional completion is cancellable and does not occupy the import entry. Late work cannot overwrite a newer document. Validate actual renderer/hit/save recovery locally.
2. Connect real startup hydration, political reconciliation and interaction infrastructure to existing task lifecycle, preserving shared resource reuse and guarding receiving state. Exercise cancellation, deadlines and late completion.
3. Bound ZIP output while decompressing, support cancellation and reuse parsed preview payload in the actual sidebar path. Cover corrupt/oversize/many-entry/cancel cases.
4. Classify and repair remaining P4 owners and read-only escapes with the frozen policy baseline preserved; verify current evidence and correct stale PR/deployment records.

## Constraints

Preserve other work. No new scheduler or project format, no widened timeout/console allowlists, no frozen baseline reset. No push, production deployment or artifact-only promotion is requested. Local tests and admission evidence must be distinguished from remote publication.
