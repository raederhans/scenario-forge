# Plan

## Stable local integration — 2026-09-07

Development scope is complete. Freeze source, verify final handoff and full changed-file routing, exclude personal configuration, and create one coherent main-branch commit containing the architecture closeout and renderer/test continuations. No remote divergence or source merge is required. Preserve historical worktrees. Remote main publication triggers Pages deployment and requires explicit deployment authorization; do not infer production delivery from local integration.

## Fill target continuation — completed 2026-09-07

Extract country/owner/parent target resolution and double-click planning as one live-state reader with existing injected identity/exclusion helpers. Preserve mutation and history execution in current owners. Consolidate source checks into behavior coverage. Completed with behavior71, scenario1, exact reader proof, scoped writer increment0, architecture/routes455, Pages build/mirror1 and diff check. Main14,811 ->14,692; no remaining work in this selected continuation.

## Three sequential renderer rounds — 2026-09-07

Completed all three rounds with per-round target tests and scoped authority checks, followed by architecture/routes/import graph, exact-reader negative tests and canonical Pages build/mirror. Entrypoint15,434 ->14,811 (-623). No remaining work in this three-round scope.

User authorized three further bounded rounds, including larger coherent modules. Round1: bathymetry/coastal-accent style decisions. Round2: country parent-grouping candidate selection. Round3: visible-frame identity/readiness decisions. Preserve raw cache/state writes and transaction commits in existing owners; preserve live dependencies, cancellation, isolation and defaults. Each round gets relevant behavior tests and scoped reader/writer proof before the next starts; final integration runs affected boundary/routes and a single Pages build/mirror. Consolidate related fixtures/source assertions; do not remove safety coverage. Sole modifying owner is this task, no other agent edits the entrypoint. Preserve all inherited dirty files, frozen baselines and budgets. No commits, push or release.

## Renderer signature continuation — 2026-09-07

Extract the cohesive render-pass cache signature policy (pass identity, transform reuse identity and political static identity) from the renderer entrypoint. Preserve lazy initialization, current-state reads and helper evaluation order. Consolidate related source-token assertions into table-driven behavior coverage for invalidation, stable transforms, instance isolation and actual renderer import. Maintain exact policy reader receipts, targeted verification routing and Pages mirrors. Preserve all inherited uncommitted work; no scenario lifecycle changes, frozen baseline changes, commit, push or release. Use scoped policy discovery/semantic checks instead of repeating the prior full historical scan without cause.

Completed this slice with focused behavior, scoped no-increment writer proof, immutable reader receipts, routes and Pages mirrors. A final forwarding-default regression was reproduced and corrected before acceptance. Further renderer responsibilities require a separate bounded continuation; this slice has no open implementation or validation item.

## Current continuation — 2026-09-07 after PR122

User requests completion of the four remaining architecture items, excluding batch integration. Audit the merged owners first; finish concrete lifecycle gaps, retire unused state relay APIs together with their migration contracts, consolidate redundant tests without losing cancellation/atomicity/isolation coverage, and reconcile the accumulated state-writer inventory through the official trusted-policy builder. Preserve personal `.codex/config.toml` changes and frozen historical baselines. Acceptance uses focused behavior and migration contracts, policy consistency, catalog routing and canonical Pages mirrors. Do not infer performance or release admission from these checks.

Completed: renderer responsibility ownership, scenario request/settlement fences, four relay retirements and proof migration, and redundant fixture/forwarding-test consolidation. Focused behavior and regression tests pass; trusted policy generation and independent checker pass with zero violations; frozen baselines/budgets are unchanged; Pages/resource mirrors pass; all changed files have routes. No remaining implementation or verification work within this continuation's scope.

## Historical round 1

Reduce renderer assembly responsibility through scenario region overlay/cache, political feature policy, urban adaptive paint, and city label text models. Preserve dynamic state/surface/projection reads, cache resets and historical state-writer anchors. Parent integrates sequentially; agents own new module/test files only.

Acceptance: focused behavior tests, affected existing contracts, architecture and policy consistency, verification catalog alignment, Pages mirrors, reviewable local commit. No performance improvement or production release claim.

## Round 2 (authorized after parent review)

Start from d8ff63b5 on the same branch. Extract political path-cache/warmup, brush gesture/session orchestration, eligible static mesh lifecycle, and physical intensity preview/point picking. Keep direct writer bodies and historical action anchors in place. Preserve lazy/live reads, schedule/cancel order, session suppression/history flush, and existing fences. Parent alone integrates main/catalog/dist. Validate lifecycle behavior with focused tests and affected contracts, then local reviewable commit; merging/release is later.

Audit round1 and round2 policy increments with per-file official discovery. Do not repeat full historical P4 scan. Apply local registration only if supported without changing frozen proofs; otherwise list each added binding and exact blocker. Policy qualification and behavior qualification remain separate.

## Round 3 (authorized after second parent review)

Extract operation graphics editor drawing and physical intensity interaction sessions; extend existing strategic helpers with defs and unit-counter interaction and existing display model with SIDC/symbol rendering cache. Preserve main selected-vertex callback and intensity normalizer writes, retired frontline writer, dynamic surfaces, draw/bind order, pointer-end commit and history/frame semantics. Parent integrates, runs focused behavior/assembly/contracts and Pages mirror, obtains bounded review and commits. Report parent and stop before round4. Per-file policy increment audit only.

## Continued batch after PR121 (2026-09-07)

Start from fetched origin/main 731afeaddb13052232dad2b003086beeea800c31 in isolated 2231 worktree. Converge cohesive renderer owners, scenario chunk/bundle/startup orchestration, and optional-layer resource lifecycle. Separate agent ownership, serial parent integration. Preserve identity/generation fences, cancellation, atomic rollback, live dependencies and frozen historical writer anchors. Acceptance: focused behavior and actual assembly, affected contracts, complete changed-file selector coverage, source/dist parity and independent bounded review. Record policy increments separately from inherited gaps; no full P4 qualification, performance claim, merge or release.
