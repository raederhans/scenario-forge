# Plan

Reduce renderer assembly responsibility through scenario region overlay/cache, political feature policy, urban adaptive paint, and city label text models. Preserve dynamic state/surface/projection reads, cache resets and historical state-writer anchors. Parent integrates sequentially; agents own new module/test files only.

Acceptance: focused behavior tests, affected existing contracts, architecture and policy consistency, verification catalog alignment, Pages mirrors, reviewable local commit. No performance improvement or production release claim.

## Round 2 (authorized after parent review)

Start from d8ff63b5 on the same branch. Extract political path-cache/warmup, brush gesture/session orchestration, eligible static mesh lifecycle, and physical intensity preview/point picking. Keep direct writer bodies and historical action anchors in place. Preserve lazy/live reads, schedule/cancel order, session suppression/history flush, and existing fences. Parent alone integrates main/catalog/dist. Validate lifecycle behavior with focused tests and affected contracts, then local reviewable commit; merging/release is later.

Audit round1 and round2 policy increments with per-file official discovery. Do not repeat full historical P4 scan. Apply local registration only if supported without changing frozen proofs; otherwise list each added binding and exact blocker. Policy qualification and behavior qualification remain separate.

## Round 3 (authorized after second parent review)

Extract operation graphics editor drawing and physical intensity interaction sessions; extend existing strategic helpers with defs and unit-counter interaction and existing display model with SIDC/symbol rendering cache. Preserve main selected-vertex callback and intensity normalizer writes, retired frontline writer, dynamic surfaces, draw/bind order, pointer-end commit and history/frame semantics. Parent integrates, runs focused behavior/assembly/contracts and Pages mirror, obtains bounded review and commits. Report parent and stop before round4. Per-file policy increment audit only.
