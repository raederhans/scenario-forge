# Russia precision stages 1–4

Authorized: preserve existing TNO feature identities, allocations and manual edits; use moderately simplified source geometry. Execute inventory, bounded pilots, explicit scenario-boundary constraints and staged derived assets. Production integration/browser release acceptance is a later stage.

1. Snapshot the current dirty scenario, not Git HEAD. Inventory raw ADM2 lineage, split families, owners/controllers/cores, city overrides and shell helpers. Record excluded raw sources.
2. Build a same-owner inland pilot and a cross-owner/split-family pilot from the pinned local source. Choose shared simplification based on coordinate counts and geometric displacement.
3. Preserve baseline owner-domain and pilot outer boundaries, existing water cuts, original feature IDs and all non-target geometry. Node shared source and constraint edges together. Reject ambiguous split identity and missing/empty output. Do not regenerate Russia ownership rules.
4. Build scenario assets serially outside data/dist; verify unchanged assignment files, complete detail chunks and selected mixed LOD combinations; measure disk/compressed sizes, time and peak memory. Expand candidate coverage only where pilots support it.

Acceptance: same IDs/order/properties; unchanged owners/controllers/cores/countries/manual rules, cities/shells and auxiliary objects; no new owner-boundary drift or target coverage holes; explicit precision IDs cover changed geometry regardless of cntr_code. Original source fidelity may be reduced with shared simplification, never by changing assignments.

Risks: old scenario coverage defects, 25 split children from six source parents, 853 changed source labels, dateline clipping, derived-build memory and stale gzip/snapshot bindings. No global rebuild or production write.

2026-09-20 continuation authorized by user: investigate source-quality issues, fix proven source processing loss, and expand staged candidates where existing assignments can be retained. Keep newly missing-ID ownership and global geographic calibration separate from internal same-ID upgrades.

## Full replacement authorization (new task, 2026-09-20)

The user authorized executing full stages 1–4 with external CLI cooperation, including local formal data application after validation. Earlier pilot-only restrictions above are historical. Preserve TNO owners, IDs, split families and city overrides; repair proven duplicate surfaces without removing exclusive territory; record unresolved source/mask/assignment questions explicitly. Build complete candidate, review regional exceptions, validate geometry/assets and localhost visual/editor behavior, then apply only verified outputs in this isolated worktree. No push/deployment.
