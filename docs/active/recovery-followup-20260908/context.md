# Context

Baseline main/origin main: 9d2d9e88bc54c0ecf61ad03dc69495600d1d5895. Previous source PR #123 required CI passed, but subsequent automatic deployment failed drift; no deployment from that run.
Primary worktree: C:/Users/raede/.codex/worktrees/recovery-r0-u1-20260908/mapcreator, branch codex/recovery-r0-u1-20260908. Original workspace personal config and untracked M0/M1 records preserved.
Collaborator finished T1 and is now released for P1. It owns the water bounds cache and its one lazy map_renderer dependency connection, the dev latency probe, and their narrow tests. Primary owns U1, dist and shared catalog integration.

## Live ownership

Primary owns canonical builder: `npm run python -- tools/build_pages_dist.py`, cwd primary worktree. No builder currently runs; wait until P1 returns browser/perf ownership. The final build must include both U1 and P1 and remain within the existing product size budget. Also refresh only the changed locales record in data/manifest.json (byte identity/counts) before the final build; do not cold-rebuild raw geography.

T1 completed in task 01a08017-f491-7ae2-8e80-27db3cb17647, commit 7508660a, integrated as 52c08e51; 4 behavior tests and direct-writer scanner pass in the primary worktree.
Windows checkout conversion left CRLF even after forced checkout/archive attempts. Source js/css/vendor/index.html were restored from `git cat-file --batch` blob bytes (verified renderer CRLF count zero), preserving semantic Git content. The intermediate build was stopped; authoritative canonical build log is .runtime/tmp/r0-git-blob-build.log. Do not use earlier build manifests.
Planned generated-product server: primary only, `py -3 -m http.server 8832 --bind 127.0.0.1 --directory dist`, cwd this worktree, log .runtime/tmp/r0-dist-server.log. It must serve only dist, with no source redirects. Stop after product checks.
Planned browser checks: `PLAYWRIGHT_TEST_BASE_URL=http://127.0.0.1:8832`, existing tests/e2e/project_save_load_roundtrip.spec.js grep `editing baseline fast`, then tests/e2e/water_cache_strategy_regression.spec.js grep `mode reuse`, workers=1, output .runtime/tests/playwright/r0-dist. Test module imports now resolve relative to /app/ so pure dist hosting works. Exit zero and actual editing/cache assertions required; failure retains traces and is investigated without budget/allowlist relaxation.

Latest canonical build after the reproduced Modern World packaging fix: .runtime/tmp/r0-modern-build.log, exit zero, 922.02 MiB. Generated app/data is ignored by design; do not force-add it. Startup shell log .runtime/tmp/r0-modern-shell.log ran 63 tests, with only the obsolete unknown_files=8 assertion failing; updated to 7 and focused rerun passed. Metadata .runtime/tmp/r0-metadata.log: 51 passed. Cache .runtime/tmp/r0-dist-water.log: one passed in 31.8s.

R0 server and browser completed/stopped. Log .runtime/tmp/r0-dist-roundtrip-final.log passed. This additional 1280x720 case uses supported keyboard history shortcuts because compact layout hides toolbar buttons, then verifies save/reload and Modern World switch. Original full 1600x1000 cases are unchanged.

R0/T1 merged PR #124, merge fbf89bee67db51bb541c8b2cf223942d738f23b5; automatic deployment 34207190613 succeeded. Current primary branch codex/recovery-u1-p1-20260908. Original main fast-forwarded while preserving personal WIP. Clean evidence worktree recovery-r0-clean-20260908 at 10edc8f7 rebuilt twice, zero drift.

U1 source and tests are primary-owned and uncommitted. Migration callback provides a transient summary (no serialized fields); funnel success observers deliver it only after apply; Project controller displays scenario/color/ownership/ignored counts and distinguishes IMPORT_ABORTED. Target behavior 68 passed. Browser .runtime/tmp/u1-roundtrip-fresh-edit.log passed in 1.2m; server PID68472 and browser stopped. One boundary assertion is expected to pass after final dist rebuild; other nine passed. Test import graph passed.

P1 task owns browser/perf sampling exclusively again as of 09:06 UTC. Port8008 listener49912, launcher44200 in worktree5d0c. Selected hotspot is water projected bounds in scenario_region_overlay_render_owner.js, with existing reset lifecycle; coverage policy shares the same getter through a single lazy map_renderer connection. No chunk-visual second hotspot. Final 8/8 runs passed (40 operation samples); follow-up busy ABBA is planned because redo first-visible samples were slower despite a faster water stage. Do not claim whole-input improvement until those results are assessed. Primary waits for release before builders/heavy tests. Final builder/PR integration remains primary-owned.

Latest ownership: P1 sampling/regr completed (12 runs, 60 samples, HOI4/ABA 2/2), server49912 stopped, all browser processes exited, ownership returned to primary. Await P1 final commit/report before cherry-pick. Primary updated only data/manifest.json locales byte identity/counts; catalog regeneration has zero catalog diff. data_health passed with report-only existing large-file warnings; tests.test_data_catalog_contract currently running in exec session7017, logs .runtime/tmp/u1-data-{catalog,health,contract}.log. Wait for it before final builder. On P1 cherry-pick, ensure map_renderer.js and overlay owner use canonical Git LF bytes; do not overwrite uncommitted U1 sources when normalizing.

Current override: P1 first delivery NOT accepted as complete: busy inputs did not overlap the real work and redo showed a potential regression. Same collaborator continues v2 actual concurrent real input/renderer calibration; local browser/perf ownership returned after U1 final builder. Its commits 1b08b219/79c81016 remain unintegrated. Primary U1 data contract final18/18, boundary10/10, canonical builder922.02MiB all passed; no primary server/browser/build/test remains running. Next: commit/push U1 independently, remote CI/merge; wait for P1 corrected evidence and acceptance before integrating its cache.
