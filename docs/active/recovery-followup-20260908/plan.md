# Recovery follow-up

Source: mother conversation https://chatgpt.com/c/6a9ec54f-31f0-83ec-953a-efa96be491b0, audit of PR #123. User authorized one collaborating task and completion of R0, T1, U1, P1.

1. R0 (primary): canonical full-source dist/manifest rebuild; clean-checkout zero drift; generated-product editing/save/reload and TNO cache reuse checks; relevant PR mirror consistency coverage and remote validation.
2. T1 (collaborating task, independently during R0): preserve real history behavior assertions while eliminating unauthorized singleton fixture writes and restoring touched state. Target behavior and direct-writer check pass, no production allowlist relaxation.
3. U1 (primary, after R0): reuse existing import audit/UI, make success/ignored data/cancellation/failure understandable without losing valid unloaded IDs or dirty state. Focused behavior and browser validation.
4. P1 (collaborating task, after R0): improve existing queue/handler/visible/stable input measurements for busy and stable conditions, then choose exactly one evidenced hotspot. Fixed same-machine interleaved A/B, raw samples, absolute operation timings and correctness preservation. No unsupported percentile/performance claims.

Primary alone owns final dist, shared verification catalog, integration and browser/build execution. No state architecture rewrite, tracked-dist retirement, raw-data cold rebuild or historical P4 admission is added to these acceptance criteria. Production status requires actual deployment evidence.
