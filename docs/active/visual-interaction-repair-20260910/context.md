# Context

2026-09-10: Start from main 31566f15 on codex/visual-interaction-repair-20260910. Untracked .playwright-mcp is previous diagnosis output; preserve until safely relocated beneath .runtime. No pre-existing tracked changes.

Evidence from previous turn: TNO selected #00ff00 in sovereignty mode assigned Milan ITC4C to GER and resolved #3c3c3c; Visual Color correctly produced the selected override. First zoom 1 -> 2.639 dropped landData 12413 -> 1633, retained only six political detail chunks and evicted coarse. Recovery window 16.391s, long tasks 5.298s and 6.473s. A later zoom recovery 4.565s had inclusive CPU applyPendingScenarioChunkPromotion 4.102s -> synchronous flush -> contextBase 3.177s. Contour host-color lookup repeats geometric matching when color revision changes. Progressive background uses topologyPrimary admin0; post-edit mixed-color sequence still needed direct validation.

Ownership: main exclusively owns map_renderer.js, chunk selector/merge/runtime, political background, render scheduling, browser/server/build/dist. fix_paint_intent owns lifecycle_runtime.js, toolbar.js, palette_library_panel.js and related tests. default_contours_off owns physical defaults and contour resource-loading paths/tests; no toolbar or renderer writes.

Live execution: main only. Existing diagnosis server and tab were closed in prior turn. Main starts `py tools/dev_server.py` from the repository with MAPCREATOR_OPEN_BROWSER=0, MAPCREATOR_DEV_PORT=8000, MAPCREATOR_RUNTIME_ROOT=.runtime; server log `.runtime/browser/visual-repair-20260910/server.log`. Port 8000 was free. Main owns the server, browser probes, Node checks, and dist build. Stop the owned server after focused localhost TNO / HOI4 / modern checks; each probe is bounded at 120 seconds, no external navigation.

Implementation: agents completed palette visual intent, default atlas_only with lazy explicit contour loading, persistent coarse political coverage with detail priority and global primary features. Main removed modern admin0 from progressive scenario recovery, schedules promotion rendering, and retains composite pixels across DPR resize. Main target checks (canvas manager, political background/orchestrator, promotion lock, render boundary) passed 33 tests. Agent target counts and browser results are pending final integration notes.

Browser findings: MCP ordinary TNO first zoom showed correct hits/colors for Germany/France/Britain/Russia. Green and magenta Milan edits changed visual overrides without changing ITA owner. Standalone `.runtime/browser/visual-repair-20260910/probe.cjs` uses fresh local Chromium at DPR 1/2, samples visible pixels during wheel zoom, performs two colors and pan away/back. TNO DPR2 passed, 0 sampled blank frames, retained edits and hits; max frame gap 2.02s. This was before the final border optimization. HOI4 ordinary first zoom exposed a 71.339s long task, so the 120s probe timed out before completing pan. The profile-only follow-up completed: `.runtime/browser/visual-repair-20260910/hoi4-profile.json`; inclusive CPU `static_border_mesh_lifecycle.js` deferred callback 66.52s -> ensureCountrySourceBorderMeshes -> buildSourceBorderMeshes 66.31s; repeated country/interaction policy resolution dominates. This is a confirmed remaining root cause, not a contour issue.

Next ownership: political_base_coverage edits border_mesh_source_selection.js plus its real-topojson equivalence/index tests, with country+shared-arc-neighbor subobjects and per-geometry classification caching. fix_paint_intent edits static_border_mesh_lifecycle.js and tests to budget/cancel country mesh slices. Main owns border_mesh_owner/map_renderer adapters (countryAssignmentRevision and requested province/local kinds, scheduled render), all browser/build/test execution. Country resolution is dynamic scenario ownership, so cache needs explicit assignment revision, not topology identity alone.

Verification: updated boundary tests for scheduled promotion and no modern admin0 passed 62 Python checks. Full toolbar boundary module has three confirmed pre-existing failures at HEAD 31566f15: export_workbench_persistence_contract_stays_stable, special_zone_membership_tools_have_explicit_renderer_modes, special_zone_persistence_contract_stays_stable; agent ran original tests against original HEAD source using git-show-backed reads and all three fail identically. Do not present the whole module as passed or alter unrelated assertions.

Border optimization integrated: geometry/arc adjacency WeakMap index, ownership-revision country groups, neighbor-preserving per-country mesh inputs, requested-kind-only construction, and cancellable country slices with an 8 ms between-country budget. A single country is not preemptible. Exact old/new topology output comparison passed 13 tests; deferred lifecycle passed 17 tests. Main adapters pass dynamic country assignment revisions and schedule the final render.

Intermediate source browser flows passed for HOI4 1936 DPR1 and modern DPR1, both zoom 1 -> 2.639, two distinct paint colors, pan to the left viewport edge, paint again there, and return with the edit preserved. Closing the visible Legend panel through its close button was necessary before the edge probe because it physically covered the Milan coordinate; this was probe obstruction, not lost geography. Later source changes required final reruns; use the final measurements below instead of these intermediate timings.

Further verified fixes: TNO complete coverage compared raw full chunk features to intentionally filtered interactive landData; 51 noninteractive Arctic shells made that permanently false. Full geometry now checks landDataFull, interactive IDs are generated by the existing buildInteractiveLandData policy, and color coverage stays strict per canonical ID. The chunk merger and promotion identity list now use canonical properties.id instead of a topology sequence id, so coarse/detail duplicates and collisions no longer distort coverage. The TNO deferred-infrastructure measurement dropped from a repeated full restore to 118.3 ms with completePoliticalDerivedStateReady=true and restoredFullPoliticalChunkData=false in the focused probe. Diagnostic reporting was moved into the existing promotion helper to keep the refresh runtime under its architecture budget. Helper + deferred lifecycle 20/20, chunk contracts 58/58, final coverage/payload 13/13, Python renderer/chunk boundaries 45/45, architecture check passed.

User added four geographic checks. Actual TNO runtime contained a modern primary RU polygon (__coveragePromoted) over the RKM/RFA Arctic shells. Complete political payloads now carry globalCoverage through normalization/equality and replace primary political geometry rather than supplementing it. Collection behavior tests passed 3/3. Progressive rendering also immediately draws visual entries absent from the interactive collection (including shells), while the full grouped cache warms; background owner tests passed 14/14. Latest source needs the final browser reruns and dist synchronization before completion.

Regional evidence: Congo internal point [18.32264,-1.105915] has water hit congo_lake and blue canvas pixels; it is intentionally cut from the land mask. Somalia 18 coarse/detail IDs and AOI owners are complete; Awdal/Bari/Lower Juba actual canvas pixels [78,138,74] match AOI #4e8a4a. Tiny coastal Banadir and the Adriatic [14,44] probe can land on adjacent raster pixels; use interior points for visual assertions. Adriatic 18238 is ITA green; adjacent 18252 is ATL beige, and [14,44] is only 0.011214 degrees from their boundary. Interior points are [13.27917465,44.17006353] and [13.83454939,44.45555538]. Atlantropa sea [18,42] correctly has water hit ATLSEA_adriatica_8597_5838_0. Arctic [55,74]/[140,75] now contain only the correct RKM/RFA shells after source isolation, no modern RU; shell geometry remains noninteractive by the existing data contract. Same-version visual override persistence supports shell IDs, but the IDs include generator order and a single ID may cover multiple islands, so independent shell editing was not enabled.

The state-write allowlist check's four unexpected test files are also pre-existing: running its actual hasDirectStateWrites function against git-show 31566f15 and current contents returns true for all four. Log: .runtime/tests/visual-writer-baseline.log. No allowlist or thresholds were widened.

Final ordinary-mode probes: modern_world DPR1 PASS, maximum frame gap 930.4 ms; tno_1962 DPR2 PASS, 2147.3 ms; hoi4_1936 DPR1 PASS, 3913.8 ms (largest individual long task 3902 ms). All retain global coarse coverage, visual edits and actual edge clickability, report zero sampled blank zoom frames, zero page errors/failed requests/contour requests, completePoliticalDerivedStateReady=true, and restoredFullPoliticalChunkData=false. Deferred infrastructure durations were respectively 324.6 / 182.5 / 113.8 ms. These are local single-run measurements, not a formal clean-head benchmark, CI, release admission, or proof of subsecond interaction. HOI4 still has multi-second work despite eliminating the earlier 71.339-second blocking border path.

Final HOI4 coverage diagnosis: 11 intentionally excluded Antarctic geometries had no political colors by existing policy. Color completeness now uses shouldExcludePoliticalVisualFeature; full geometry completeness remains mandatory, so this does not excuse missing land. Helper/deferred lifecycle tests passed 20/20 after this change. Browser readiness now requires the requested scenario with real land data and completed startup unlock/promotion; the prior fixture occasionally measured an empty starter frame as ready.

Final region probes (water and land, TNO at 450%) completed without page errors. Adriatic internal points select exactly ATLPRV_18238 / ATLPRV_18252 and show green / beige as configured; island and sea samples hit their respective land/water IDs. Congo selects congo_lake and shows [45,71,105,255]. Arctic points contain only RKM/RFA shell geometries, with dark scenario colors visibly filled, no modern RU overlay. They remain noninteractive by the source contract. Somalia Awdal, Bari, Lower Juba have exact geometry/hit/color agreement. Tiny Banadir at this scale can resolve to an adjacent raster hit; no blanket coastal selection-precision claim is made. Source coarse/detail inventories are complete for the sampled regions; no geography data rewrite was necessary.

The previous diagnosis output .playwright-mcp was preserved under .runtime/browser/visual-repair-20260910/diagnosis-mcp. All one-off probes, screenshots and logs remain below .runtime.

Close-out: canonical tools/build_pages_dist.py succeeded and synchronized the 22 changed runtime modules plus dist/pages-dist-manifest.json (920.72 MiB artifact). tests.test_pages_dist_startup_shell passed 63/63; the final scenario chunk / political background / border owner Python boundaries passed 45/45; the architecture boundary check and git diff --check passed. Earlier focused behavior checks cover paint intent, contour defaults/lazy loading, canvas preservation, promotion identity/lock handling, global coverage, scenario-only background rendering and indexed/cancellable border construction. Known unrelated toolbar/state-writer baseline failures remain as documented above. No allowlist, performance baseline or architectural size budget was relaxed.

The owned localhost server (Python PID 8112, verified dev_server.py under its owned launcher) was stopped after all browser probes. MCP and standalone browsers are closed. Changes remain local and uncommitted on codex/visual-interaction-repair-20260910; no push, merge, or deployment was performed.

## Follow-up performance research, 2026-09-10

User confirmed most visual defects are fixed and requested research into remaining drag, zoom and paint latency, permitting relevant architectural changes. This pass did not edit runtime source or dist. Prior authorized preview server was restarted and remains on port 8000 (observed Python PID 25500, exec session 82045). Main exclusively owned the profiling browser and reused that server; the user's in-app page was not driven or reloaded. Existing agents performed bounded read-only call-path research. Independent browser processes were closed after the probes; keep the user's preview server running.

Live command: node .runtime/browser/interaction-perf-20260910/profile.cjs tno_1962 1, then tno_1962 2 steady; cwd is this checkout. Each probe has a 120-second cap, localhost only, viewport 1280x720, balanced ordinary mode, contours off; no render_diag, diagnostic profile or concurrent CPU workload. CDP CPU sampling interval 2000 microseconds; requestAnimationFrame intervals and existing metric changes recorded. These callback intervals are not a visual FPS guarantee; CPU sample times and individual local runs are approximate, not release/perf-gate evidence. The first DPR1 cold-pan interval overlaps deferred startup hydration and is not representative of a fully warmed session.

Measured evidence:
- Warm DPR1 200px pan/return: input RAF p95 around 20-26ms, but recovery long tasks 0.9-1.6s. Each pan triggers political promotion about 0.86-0.97s, land collection rebuild about 250ms, and usually two hit canvas builds totalling 300-460ms. Continuous interaction is worse because the next gesture arrives while these tasks run.
- TNO DPR2 at zoom 2.639: even a 20px pan increments topology/color revisions (9/12 -> 10/13) while land count stays 12413. Short-pan promotion 948ms, return 1008ms. Four alternating 180px drags with 180ms gaps take 11916ms to dispatch/finish the input sequence; long tasks cumulatively 11350ms, largest 1951ms.
- Ordinary fill at the same transform: click command around 99ms; later long task 1455ms. Internal patch metric 6.7ms is not total physical input-to-display latency. Partial political repaint rejects reference-transform-mismatch and a 144ms hit canvas rebuild follows.

Confirmed causes and source pointers:
1. chunk_runtime.js:2808 still treats politicalVisibleFeatureSubsetSignature changes as a payload change, adds political to changed layers, and :1642 forces refresh despite identical full payload references. Global coverage introduced earlier bypasses viewport merging but had not removed this trigger. Pure camera movement therefore causes full collection/color/index/mesh rebuilds. Keep actual detail replacements, legacy subset mode and startup force semantics intact.
2. shouldAllowZoomEvent(map_renderer.js:13872) lets ordinary left-button fill clicks reach d3.zoom. Actual local d3 source emits start on mousedown and end on mouseup even without a move. zoom_interaction_lifecycle_owner.js:104/134 then enters interacting/settling, switches DPR, rebuilds snapshot/last-transform frame and schedules recovery. Delay this expensive preparation until the first actual transform change; track whether any movement occurred even if the gesture eventually returns to its origin. Preserve wheel/touch/programmatic zoom and left-button dragging in fill mode.
3. political_path_cache_owner.js:30/85 requires full camera transform, viewport and DPR for Path2D reuse. The actual path built at :145 contains only base geoPath projection; political_partial_repaint_owner.js:508 applies DPR and camera on replay. Introduce projection/path generation, retain geometryRef and scene/source correctness, and separate viewport-bound warmup identity from geometry cache identity. Current projection signature rounds only scale/translate and omits clip/precision, so it is insufficient alone.
4. borders signature includes global colorRevision. Auto internal border stroke truly depends on dominant country fill (map_renderer.js:5204/5254), so removing this dependency wholesale would be wrong. However buildRenderableBoundaryMesh(border_draw_owner.js:205) repeats simplification/projection on recolor; split immutable geometry caching from stroke revision, incrementally maintain affected country dominant colors. DPR1 fill profile attributes about 235ms to this mesh transformation; after lazy-click prototype the residual DPR2 fill still spends about 135ms in border drawing, including 77ms mesh transformation.
5. Full exact composition remains expensive: controlled DPR2 short-return profile samples about 823ms in composeRenderPassesToTarget, 483ms political pass, 285ms borders. These nested/cumulative values must not be summed as independent wall time. Cache normalizer shape validation costs about 194ms cumulatively in that trace; getRenderPassCacheState is repeatedly used within feature paths. Move validation to creation/replacement boundaries or pass a validated frame-local cache, retaining external mutation/restore contracts.
6. Exact scheduler queues already check generation/idle and are canceled by new gestures. Chunk promotion only checks gesture phase on entry; after await yieldToFrame its continuation checks scene/request/selection identities but not the new gesture. A new gesture need not change selectionVersion immediately, so old promotion can continue into it. A unified gesture generation and atomic staging/publish boundary would make recovery cancellable without exposing half-applied state.

Isolated experiments (response rewriting only in a separate Playwright context; no product source writes):
- Initial global no-subset prototype failed the 20-second startup settle condition before any measured action. The subsequent controlled experiment enables the intervention only after startup and unmeasured warmup, so it does not validate changing startup behavior.
- Controlled no-subset-force DPR2: 20px pan/return no longer increments topology/color and records no political promotion; hit canvas builds drop from two to one. Four alternating pans take 7536ms vs 11916ms in the separate baseline run; cumulative long tasks 6212ms vs 11350ms. Largest remaining task 1596ms, so this alone does not make dragging smooth.
- In that same page, stationary-click A/B/A: normal gesture fill largest task 1479ms, partial rejected reference-transform-mismatch, hit build 148ms; lazy gesture prototype largest task 293ms, successful partial repaint 1.9ms, no hit rebuild; re-enable old gesture lifecycle and largest task returns to 1461ms, partial rejects and hit build returns. This supports causal attribution beyond cache warmup. No assertion of complete visual parity, touch parity or production readiness is made by this prototype.
- Evidence files: .runtime/browser/interaction-perf-20260910/{tno_1962-dpr1.json,tno_1962-dpr2-steady.json,tno_1962-dpr2-steady-no-subset-force.json}, per-action .cpuprofile files, profile.cjs and compare.cjs. All completed measured runs report no page errors.

Recommendation: first fix the two false lifecycle triggers; then separate geometry, appearance and camera revisions, reuse Path2D/mesh across camera and color changes, and consolidate recovery transaction scheduling. Only after reducing duplicate work move remaining pure geometry/projection/raster batches to a persistent worker. Existing political_raster_worker_client protocol v4 already has stale-result identities but is default-off; packet projection currently happens on the main thread, so merely enabling it cannot remove that cost. Chromium RenderingNG architecture and web.dev OffscreenCanvas guidance support off-main-thread work, but compositor-only CSS transforms do not automatically make JS-driven map input independent of the main thread.

Follow-up acceptance should cover repeated real drags and overlapping gestures (not only a settled single operation), stationary click never changing camera phase/DPR, pure pan never changing data/color revisions, geometry cache reuse under pan/color with correct invalidation on fit/clip/precision/scene/geometry change, no duplicate hit rebuild, and preserved color/hit/shell/undo/export correctness at DPR1/2. No gate threshold, baseline, allowlist or current production behavior was changed by this research pass.

Implementation started on user's explicit instruction to finish layers 1/2 then separately assess layer 3. Ownership: fix_paint_intent owns zoom_interaction_lifecycle_owner and tests; political_base_coverage owns political_path_cache_owner, border_draw_owner, projection/fit owners plus a focused projection identity helper and tests; main owns chunk runtime/selection, map_renderer and all shared adapter/state/signature integration, country fill appearance, cache getter, hit deduplication, records, browser/build/dist. Only main runs live browser/profile/build; agents may run small isolated behavior tests until browser measurement starts. Reuse the already-running preview server on port8000 and leave it available for user. Main probe commands under .runtime/browser/interaction-perf-20260910, subsequent implementation-specific outputs under .runtime/browser/interaction-perf-implementation-20260910; all browser runs are bounded localhost quick probes. Do not modify the user's current browser document.

Layer 1 implementation: zoom start stays lazy until a real x/y/k change; final-frame scheduling retains the last actual transform and rejects stale callbacks. Global complete political payloads ignore viewport-only subset changes, including the normalized null visible-subset representation. Real layer payload replacement is detected with chunk ID plus payload identity; startup, LOD and legacy subset paths remain active. Promotion continuations detect a new gesture even when it ended during the await, restore owned infrastructure roots, and defer the pending promotion. Gesture-stage DPR changes were removed while keeping profile/device limits, avoiding twice-per-gesture canvas and reference invalidation. Split exact recovery now works for supported active passes in ordinary modes; signature-only changes are included in the sliced plan. The defer flag remains set through all applying slices, preventing incidental redraws from synchronously rebuilding unfinished passes.

Layer 2 implementation: projection generation and geometry identity control political Path2D, projected bounds, ocean base paths and simplified border meshes. Camera/DPR/color changes reuse those geometries; source replacement, projection refit and changed geometry invalidate them. Exact political drawing populates missing paths immediately, rather than relying on the limited idle warmup. Country dominant-fill counts update only affected countries and advance border appearance revision only when the visible dominant color changes. Canonical paint writes no longer cause a redundant full legacy-map migration on every click. Render-cache validation has a synchronous frame-local scope, with root replacement and normal outside-scope validation preserved. Hit rebuild scheduling uses task identity and scene/camera currentness; stationary paint does not rebuild the hit canvas.

Targeted integration evidence: .runtime/tests/interaction-layers-targeted.log has 132/132 passing Node tests; .runtime/tests/interaction-layers-final-recovery.log has 47/47 passing recovery/cache/hit/background tests. The related Python boundary group has 55/56 passing tests. The remaining test_map_renderer_border_draw_owner_boundary_contract assertion expects old getContext/pathCanvas/projection and border mesh tokens already absent from HEAD 31566f15; a read-only HEAD comparison confirmed this baseline debt. No source-assert allowlist or architectural size budget was relaxed. The temporary attempted modernization of that stale test was undone; it has no content diff.

Final ordinary source visual probes passed for TNO DPR2, modern_world DPR1 and HOI4 1936 DPR1. Each verifies real green/magenta/orange UI paints without sovereignty change; stationary clicks leave camera, DPR, phase and hit-render count unchanged; zoom and viewport-edge hits remain present; paint-then-immediate-drag preserves the visible pixel; Ctrl+Z / Ctrl+Shift+Z restore the expected colors. All three report zero page errors, failed requests and sampled blank frames. TNO France/Russia pixels remain scenario colors. The tiny modern Milan sample blends with an existing antialiased boundary; the probe therefore compares the same pixel before/after dragging and undo, rather than incorrectly demanding an unblended RGB value. TNO's ordinary cold-zoom maximum long task was 1410ms, HOI4's 1550ms and modern's 221ms. These cold geometry/detail costs remain unresolved and are not hidden by the successful visual assertions.

Canonical dist build succeeded at 920.74 MiB and the formerly failing dist-mirror inventory assertion now passes. Architecture boundaries and git diff --check pass after synchronization. Relevant output: .runtime/tests/interaction-layers-dist-build.log and interaction-layers-dist-mirror.log; visual output: .runtime/browser/interaction-perf-implementation-20260910/*-visual.json. The user's existing localhost preview remains running; only independent probe browser processes were closed.

Final dist startup suite passed 63/63 (.runtime/tests/interaction-layers-dist-startup.log). Final production-source profiles completed in ordinary balanced mode at DPR2 steady and DPR1 full, with no page errors and no response rewriting. DPR2 four-pan input sequence improved from 11916ms to 3355ms; maximum RAF gap from 1954.5ms to 304.9ms; long-task sum from 11350ms to 603ms. Stationary fill click acknowledgement was 39ms, subsequent longest task 89ms, partial repaint 2ms and no hit rebuild. Pure pans retained topology/color revisions with no political promotion. Short-pan recovery still has about 320ms tasks. DPR1 cold zoom still has a 1306ms task, source-border mesh CPU about 731ms cumulative and real political promotion about 751ms; these are overlapping costs, not additive. One initial profile timed out waiting for startup stability before actions; the failure was retained, timeout unchanged, and diagnosed reruns completed. Its cause remains unknown.

Third-layer assessment was performed separately after the implementation checks and final profiles. See worker-assessment/plan.md for evidence, the persistent geometry/hit/raster ownership proposal, existing-worker limitations, and prototype acceptance. No Worker flag or implementation was changed. All owned final browser and test processes finished; the user preview server remains on port8000.

User now authorized third-layer implementation. Main owns map_renderer, raster client/integration/composition, shared registry/build, records and all live processes. fix_paint_intent owns new border mesh Worker client/kernel/entry plus optional async static-border lifecycle ports/tests. political_base_coverage owns new D3 raster kernel/entry/tests. Preserve all previous uncommitted work. Main reuses port8000 PID25500; no user tab navigation/reload. Standalone Playwright probes use .runtime/browser/interaction-worker-20260910, max120s per quick probe; command/cwd: node <probe>.cjs from this checkout, success completed assertions/profiles, failure assertion/page error/deadline. Only main runs browser/profile/build and monitors their stable logs; agents run isolated Node tests only. Final build py -3 tools/build_pages_dist.py and corresponding startup/mirror checks run sequentially after source freeze, logs under .runtime/tests/interaction-worker-*.log.

Third-layer integration checkpoint: persistent border topology worker and D3 raster worker are wired to ordinary rendering. Geometry upload ACKs and frame-currentness are separate; hit/political retain at most one active plus latest per kind. Worker canvases are reused. An initial prototype yielded every 64 features via nested timers and had excessive round-trip time; replaced by an 8 ms work budget with scheduler.yield where available. A real client A-B-A regression identified duplicate Promise consumers closing the same bitmap before current publication; runtime now reuses the single in-flight task per identity. Targeted kernel 7, client 6, runtime 8 tests pass at this checkpoint. Exact composition remains under measured prototype assessment, not enabled by assumption.
Measurement correction: current Playwright predicates must be synchronous after an awaited page.evaluate import. An async predicate returned a truthy Promise, so earlier startup-readiness probes could begin before ready. Current probes fixed; historical first/two-layer scripts/results preserved. One corrected worker prototype run still hung applying after A-B-A, now addressed by the bitmap ownership regression; browser confirmation pending. Future current probe settle also observes geometryWorkerQueue.pending, so a quiet main thread is not mistaken for completion of worker raster tasks.

Third-layer visual confirmation: TNO DPR2, modern_world DPR1 and HOI4 DPR1 paint/pan/undo checks completed. Modern has two existing missing startup localization URLs, documented precisely in worker-results/context.md rather than hidden. Corrected overlap probe PASS: actual pending worker at both TNO→HOI4→TNO switches, real colors in each scene, DPR1.25 physical dimensions, resize-fit then restored zoom and pan-return preserving the selected feature/color. Cached political color and layered final composition are asserted separately. A near-boundary sample and resize fit previously made the probe pick a neighboring tiny province; both worker on/off agree and current probe proves geometry interior before asserting exact color.

Region regression uncovered an old independent zoom-end demotion bug: required semantic water/relief moved to optional and were never loaded by the required-only loading chain; resulting null payload cleared scenario water. Both worker on/off reproduce missing Congo Lake. Main now protects the existing resolver's required semantic layers; test fixture uses production resolver and all 6 controller tests pass, including water/relief state commits and no repeated political promotion on pure pan. Actual post-fix Congo Lake retains its water feature, correct blue pixel and congo_lake hit. Adriatic land/sea, Arctic noninteractive shells and Somalia interior IDs were also inspected. Initial third-layer five warm and two cold profiles predate this fix; final full-water profiles use final-on/final-off labels. Do not present earlier incomplete-water timing as final-source timing.

Third layer complete locally. Full-water final DPR2 off/on each two runs: four-pan max RAF median 382.7→213.6ms, long-task sum 1165→615ms; total measured action+settle 5669→5374.9ms. DPR1 cold zoom one pair still has max long task 1912→1731ms. Fill is not proven faster: DPR2 max-long median 123→169.5ms with one on-run GC-heavy sample. Settled browser process working set adds about148–212MiB after the warm sequence; not a peak-memory/no-leak proof. Full values and limitations in worker-results/context.md and .runtime/browser/interaction-worker-20260910/final-comparison.json. Exact compositor prototype retained main composition based on total-latency regression for full changed-layer transfer.

Final checks: 170 worker/render Node tests plus 54 chunk/export/dist-contract Node tests passed; architecture and diff whitespace checks passed. Canonical dist build completed (921.00MiB); 101 startup/chunk Python tests passed. One explicit --output-root tracked-dist invocation rejected before writes; corrected to the existing no-argument build path, no tool-rule changes. All owned probe/build/test processes ended; preview PID25500 stays on8000 and final localhost HEAD was200. No commit, push or publication performed. Preserve unrelated user/other-agent data edits and historical WIP.


## Authorized merge preparation (2026-09-10)

The user authorized committing, pushing and merging the renderer work together with the completed TNO Russian/US place-name work. Source commits are 4167283b (1,082 reviewed name identities) and b21af0e6 (visual correctness and three performance layers). Regenerated startup support/bundles were byte-identical; retained the reviewed language-specific locale payloads and updated the scenario snapshot/audit/manifest from final input bytes. Strict TNO and both name rebuild-consistency checks pass. Updated source-bound delegation proofs and stale boundary fixtures to the actual owner/transaction chain; mutation permissions remain unchanged. Added an explicit locale builder test route and included both new label tests in the existing reviewed-label route. Canonical Pages output is rebuilt at 921.38 MiB. Remote protected-branch checks and merge remain pending at this preparation checkpoint.
