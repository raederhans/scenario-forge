# Visual interaction repair

User authorized fixing zoom stalls/black frames, paint intent, lost political coverage/hits, and TNO fallback colors, including changes to the relevant architecture. Contours should default off until dedicated optimization.

## Scope and acceptance

1. Selecting a color paints that color without changing ownership. Ownership editing remains explicit.
2. New default maps use atlas terrain without contours; explicit project contour settings remain supported and opening contours still works.
3. Political coarse coverage remains available independently of detail budgets. Detail replaces matching geometry without losing other countries or their hit data on zoom/pan.
4. Scenario recovery never substitutes modern admin0 political geography. Political geometry changes do not synchronously force unrelated context redraws.
5. Targeted behavior tests and focused localhost TNO/HOI4/modern interaction probes verify the final behavior. Report actual timing and visual gaps; no gate/baseline relaxation.

No remote publication, unrelated framework migration, or contour algorithm overhaul.

## Authorized follow-up implementation

User requested completion of the first two researched optimization layers, followed by a separate assessment of worker migration. Preserve all earlier visual repairs and uncommitted work.

1. Stationary clicks do not enter camera interaction/recovery. Real pan/wheel/touch/programmatic zoom retains final transform, phase and cancellation correctness.
2. Global political coverage ignores viewport-only subset signatures for data promotion; real LOD/geometry replacements and bootstrap/legacy behavior remain valid.
3. Geometry paths and border simplification use projection/geometry identity independently from camera/DPR/color. Warmup currentness remains viewport-dependent.
4. Country fill appearance updates incrementally; border geometry and automatic stroke color use separate dependencies. Normalization and hit infrastructure avoid duplicate work without weakening state/picking contracts.
5. Recovery continuations respect new gestures and preserve atomic data publication. Profile and verify repeated pan, paint-then-drag, zoom, undo, scenario coverage and startup at DPR1/2.
6. Synchronize necessary dist artifacts; only after the first two layers are verified, assess remaining worker candidates using new profiles. Worker migration itself is not authorized as part of this requested third-layer assessment.

## Authorized third-layer implementation

User subsequently requested continuing with third-layer improvements. The assessment above remains the historical decision record; Worker implementation is now authorized. Implement persistent geometry/border and hit/political raster work off the main thread, preserve D3 stream semantics, visual-shell versus interactive coverage, edits/history and stale-result rejection. Integrate exact drawing/composition only where ordinary-mode evidence supports a net improvement; retain correct unsupported-browser behavior. Verify cold/warm actions, overlapping gestures, scenario changes, DPR, undo and geographic coverage before final dist synchronization. Do not alter performance baselines or architecture budgets.
