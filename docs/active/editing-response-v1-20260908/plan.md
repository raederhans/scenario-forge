# 编辑响应与连续操作 v1

来源：用户指定的项目治理与恢复计划群最新补充方案（2026-09-08），基线 fb28b187。

本批执行 N1–N4：N3 在分段归因后决定实施或停止；N4 对已复现的生命周期缺陷实施修复，并完成代表窗口验证。此前 R0/T1/U1/P1 按原交付边界承接，不重复验收。

- 主代理：N1 post-ready scheduler 执行实例与排队回调隔离，相关测试；共享路由、最终镜像和整合。
- 子代理 n2_input_evidence：N2 实际输入探针判定 helper、轻量 Node 测试、既有原始样本归因；提出 N3 实施或停止结论。
- N1 验收：真实 scheduler 的旧 Promise 完成/拒绝不能清除 reset 后同名新任务；旧排队回调不能执行或删除新句柄；正常结束和失败仍释放任务。新增反例先失败后通过。
- N2 验收：缺失事件不算零、无 overlap 不通过 busy、模式分开、wheel 单列、保留异常、跨操作不误关联；真实探针复用 helper；三类慢现象逐项归因或列出缺口。
- N3 若触发：先固定预算，再做单热点；至少四个相邻 A/B 对、自然后台窗口和 HOI4 正确性对照。若主要是输入前 full refresh 或 UI 成本小，停止该优化。
- 所有性能浏览器/服务器、重型验证与构建串行，由主代理分配资源。生产源码冻结后统一处理 dist，不逐次全量构建。
- 不改全局状态体系、scheduler 优先级/quiet/retry 策略，不扩大 allowlist，不做发布体系迁移；不推送或生产发布。

## 剩余任务执行授权

用户随后明确要求完成原对话剩余任务。N3/N4 不再停留在列出采样缺口：补有限分段采样，按原方案触发/停止条件形成最终判定；符合条件的实际缺陷与单一热点完成实现和验收。

- 子代理 n2_input_evidence 继续负责 N3 归因、单热点、四个相邻 A/B 对、自然窗口与 HOI4 对照；该阶段独占 localhost 浏览器/服务器/性能资源。
- 主代理先静态分析 N4，准备确定性生命周期用例；在 N3 冻结测量完成前不修改其基线生产源码。随后接手 N4 的冷/热/场景切换代表窗口、完成/取消/失败状态验证和必要修复。
- 不混入旧 S1/P2/U2 方案中的新产品扩张；以最新补充 N1–N4 为执行范围。最终更新镜像和对应检查，所有未归属WIP保留。
