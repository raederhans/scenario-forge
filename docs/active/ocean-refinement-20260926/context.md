# 执行上下文

主工作区有大量并行 UI/Atlantropa WIP，未修改。实施位置 C:/Users/raede/.codex/worktrees/ocean-refinement/mapcreator，分支 codex/ocean-refinement。

Root 唯一拥有构建、契约同步与浏览器进程。输出 .runtime/reports/generated/ocean-refinement/，stage .runtime/tmp/ocean-refinement/。启动前补充完整命令与日志，任何几何/来源验证失败即停止该候选的采用。独立 worktree 不使用主工作区端口或输出。

SeaVoX 官方仍为 v19 2023。WFS 2026-09-26 查询发现未建模：Florida Strait 24043、Flores 24126、Bali 24125、Sunda 24127、Sumba 24123、Sawu 24124、Tomini 24134、Bone 24129、Aru 24128、Ceram 24132、Berau 24131。均需实际多边形验证。八个 Atlantic/Pacific sub_region: 24047,24178,24039,24040,24115,24116,24102,24103。

Root source fetch/build: python -B -u tools/build_marine_refinement_sources.py --refresh ; cwd isolated worktree; log .runtime/reports/generated/ocean-refinement/sources.log. Success exit0 and 19 fetched records plus shared source collection. Stop on upstream or geometry error.

Source refresh passed 19 records, 128 shared features. Rebuild offline with restored NE coarse source to avoid source/output feedback: python -B -u tools/build_marine_refinement_sources.py ; log sources-offline.log; root owns process. Next geometry stage command python -B -u -m tools.rebuild_water_geometry --refine-marine --stage-root .runtime/tmp/ocean-refinement/stage1 ; log geometry1.log. Stop at any final decoded geometry error; adopt only completed outputs manifest and unchanged baseline identities.

Root read-only baseline contract check while geometry stages separately: python -B tools/check_scenario_contracts.py --strict --report-path .runtime/reports/generated/ocean-refinement/contracts-before.json ; log contracts-before.log; exit code/report separates pre-existing failures from this work.

Checkpoint: baseline all six scenario strict contracts PASS (contracts-before.json). npm ci --ignore-scripts installed isolated test dependencies; no lock changes. Target node color + water fill 10/10; sidebar boundary Python13/13; marine source/authority Python7/7. Source fetch then offline build passed, shared source128 features ~20.6MB + supplement19 features ~6.1MB. Geometry stage1 running under exec session8490/python PID24972, first base topology compile/encode. Expected base167 / TNO160. No staged adoption yet. Source hashes registered in source_ledger, output source in manifest. Main workspace untouched.

Root local server: python -B -m http.server 8009 --bind 127.0.0.1 ; cwd isolated worktree; log .runtime/browser/ocean-refinement/server.log. Owned by this task; stop after browser validation. In-app browser binding browser, tab oceanTab; not yet navigated to app.
Stage1 rejected: source seam overlaps and avoidable protected-water resnapping; TNO re-clipping existing spherical coast produced D3 mismatch. Stage2 adds explicit source seam exclusions, exact protected-feature transplantation, shared base water across LODs, and compiles only changed TNO features without re-clipping existing coasts. Command python -B -u -m tools.rebuild_water_geometry --refine-marine --stage-root .runtime/tmp/ocean-refinement/stage2 ; log geometry2.log; root sole owner.
Target validation: geometry/compiler/authority unit27 PASS; polar12 PASS. Exact-water transplant keeps protected coordinates and foreign objects unchanged, with canonical base ordinary-water arcs reused by detail topology. Seam reconciliation uses explicit basin/strait pairs, not feature order; post-encoding interior checks account only for the recorded quantization cell. Local preview now uses tools/dev_server.py with MAPCREATOR_DEV_PORT=8009, MAPCREATOR_OPEN_BROWSER=0 and revalidate-static (owned session35066).
Stage2 PASS and adopted five outputs after exact input identities and 78 protected-feature equality checks. Root runs python -B tools/check_scenario_contracts.py --strict --write-safe --report-path .runtime/reports/generated/ocean-refinement/contracts-after.json ; log contracts-after.log. Regenerates only derived startup/chunks/ledgers/snapshots, all six baseline contracts previously PASS.
Generic --write-safe stopped because it resharded unrelated HOI4 political chunks. Only task-generated HOI4 changes were restored; generated untracked shards were removed within the verified isolated chunks directory. Replaced with tools/sync_marine_refinement.py: water-only TNO chunk helper plus apply_safe_scenario_contract_repairs(rebuild_chunk_assets=False) for dependent startup/hash/snapshot updates, followed by read-only --strict. Final Python geometry/partition tests31 PASS.

Final checks: final-python.log31 PASS; final-node.log22 PASS; polar-unit.log12 PASS; tno-water-tests.log73 PASS; contracts-after.json all6 PASS; data-health.log exit0; catalog tests18 initially PASS plus landing count fix targeted1 PASS. e2e-final.log2 PASS after restoring explicit open-ocean toggle authority over legacy noninteractive ocean defaults. pages-build.log697.62MiB PASS. git diff --check PASS. Browser E2E verified canvas pixels, selection, paint and undo. Supplemental in-app DOM confirms Flores entry; in-app locator interactions hit short tool deadlines, so no extra selection claim from that surface.
