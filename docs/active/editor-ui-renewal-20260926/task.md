# 当前进度

- [x] 阶段 1：明显问题修复与目标检查。
- [x] 阶段 2：城市图层属性样板与底栏。
- [x] 阶段 3：全局入口、左侧浏览与右侧属性迁移。
- [x] 阶段 4：工作台统一、双语/尺寸/回归验证。

## 验证证据

相关浏览器用例共 24 项最终通过，分批执行：新工作区 9 项、原有 shell/支持面板/交通/标注 14 项、main_shell_i18n 1 项。失败后按证据修复并仅复跑受影响用例，最终账号/城市双语/剧本菜单 3/3 通过。

窗口覆盖 1920×1080、1440×900、1366×768、1280×800、1024×768 和 768×900；验证页面无横向溢出及抽屉焦点恢复。真实城市控件写入、水域选择保留、导出画布和交通应用均有行为断言。

目标 Node 组：bootstrap/语言/选择器 24/24，runtime hook/支持弹窗 21/21，特殊区域/战略/状态 39/39，支持账号 31/31，交通 owner 31/31；收尾水域文案/账号语言/应用提示 4/4。边界 Python 34/34、交通 manifest 20/20。词典一致性、JS 语法与 git diff --check 通过。这些为分组结果，未将重复用例累计为总数。

## 实施阶段的验证边界

实施阶段尚未执行发布构建、推送或部署；后续授权后的集成验证见下节。project_support_diagnostics 静态检查存在 3 条已核对的原有源码字符串断言失败及 3 条源码与旧 dist 不一致；后者包含本轮合法源码更新，不能称全部为既有失败。runtime_hooks 静态组 5/6，通过旧源码复核确认剩余 HGO handleMouseMove/scheduleMouseMove 断言差异先于本轮存在。未修改这些无关基线或伪造全量绿色结论。

完整说明与界面对照：`.runtime/reports/generated/ui-renewal-20260926.md`。

## 推送合并阶段

- [x] 创建隔离集成分支，提取 UI 增量并保留其他工作区 WIP。
- [x] 在最新主线基线运行 91 项 Node、119 项 Python、70 项结构工具检查和 11 项浏览器用例，全部通过。
- [x] 补齐 E2E 清单与依赖图；清单、导入图及超时检查通过。
- [x] 完整生成 Pages dist；原有源码与 dist 不一致检查恢复通过。
- [ ] 推送并创建 PR，通过必需检查后合并；最终状态以 GitHub PR 回执为准。

合并条件可按用户授权适当放宽，但不关闭分支保护，不隐瞒仍存在的 4 条旧源码字符串断言失败。工作树保留运行证据用于合并复核，主目录未归属改动保持原样。
