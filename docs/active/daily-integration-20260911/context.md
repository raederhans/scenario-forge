# Integration context

2026-09-11: root is sole integration/build/test owner. All other relevant Codex tasks idle/notLoaded. Current cwd C:/Users/raede/Desktop/dev/mapcreator. User authorized all today's WIP and remote merge/push. origin/main d25da73a contains PR131 and has identical tree to previous HEAD5d656c9c, allowing safe branch switch with WIP. Contours worktree clean/already integrated; retain for existing task. No active overlapping test/build owned by this task yet; existing user dev server retained.

Protected main requires PR Verify Required, perf-gate, transport-contract-required and strict-scenario-contract-review for hoi4_1936/hoi4_1939/tno_1962. Main merge triggers existing Pages workflow; do not bypass gates. No open PR at start.

Earlier local work: Atlantropa final candidate-v2 canonical/dist and focused E2E passed; ocean compiler/masks/data and focused E2E passed; UI desktop/tablet focused E2E passed. Existing failures disclosed: landing catalog English copy mismatch, five stale UI source-location assertions, three UK water probes, blank_base ownership contract. Current broader authorization includes reconciling directly affected integration contracts. Landing fallback HTML and test now aligned to adopted UI English phrase while preserving numeric/source assertions.

Runtime logs/selector under .runtime/tmp/daily-integration-20260911. Root owns subsequent exact commands recorded here before launch. Preserve .playwright-mcp historical diagnostics and all ignored runtime/source caches; never stage those.

Root starts canonical Pages builder: py -3 -B tools/build_pages_dist.py, cwd repository, log.runtime/tmp/daily-integration-20260911/pages-build.log. Owns dist output exclusively. All current source/dist WIP authorized for integration, source files are delivery authority; historical .playwright-mcp excluded. Success canonical build within existing1024MiB budget; any failure inspect before retry. Other agents/tasks may only read completed log snapshots. Subsequent adaptive tests own.runtime test reports and run after builder completes; no parallel browser/performance sessions.

Pages copy completed; Windows transient EINVAL while overwriting LF-normalized file then manifest. Repeating only failed finalization stages succeeded without product retries or source changes. Canonical final manifest959.61MiB, under1024MiB gate. Routing completed with no unmatched files; added50-spec manifest entries and real heavy-dependency test routes, retaining main-thread resource policy. Root starts CI-equivalent adaptive child-safe checks: node tools/run_adaptive_tests.mjs --changed-files-list .runtime/tmp/daily-integration-20260911/changed-files.txt --execute --defer-main-thread --json-out .runtime/tmp/daily-integration-20260911/adaptive.json --md-out .runtime/tmp/daily-integration-20260911/adaptive.md; cwd repo; log adaptive.log. Owns runtime test outputs; any behavior failure investigated, no gate relaxation.
Adaptive v2 passed17 commands then found duplicate identical UI key 3. Set the active owner; removed redundant new definition, keeping existing text. Root resumes only failed/unexecuted commands through exported executeAdaptivePlan with continueOnFailure=true; prior passed evidence retained, outputs adaptive-remaining.json/log, script run_remaining.mjs. No verification policy altered. Optional performance harness moved to tests/e2e/dev/tno_atlantropa_refinement_perf.dev.spec.js; two normal E2E tests now use60s timeout; no allowlist widening. Canonical root E2E count49, import graph60 including dev specs.

Integration correction: the three UK probes were inherited from the same-day Atlantropa baseline, but did not fail on remote main d25da73a. The adopted ocean compiler clips named waters to physical land_mask. All three former probe coordinates are inside the final rendered mask; Humber and Cardigan were also inside the remote-main mask. Swansea/Humber differ from fine political coast geometry, so this remains a physical-mask precision limit, not a real-geography accuracy claim. Replacement interiors are inside both old and final named waters and outside final land by 0.067/0.024/0.260 degrees. Original coordinates remain explicit D3 land-exclusion probes alongside three positive bay probes; all ten runtime geometry tests pass. Validator and Python coverage probes use the stable interiors.

Full geospatial integration run: 178 tests and 6 subtests passed; two duplicate invocations exposed six lake cutout overlaps in land_mask. Scoped ATL rebuild used newly encoded global lake boundaries while retaining scenario_water. It now takes cutouts from preserved scenario_water. Final data correction removed 0.001444668971 square degrees from land_mask only; every other decoded topology object was verified unchanged before compaction. Six lake overlaps are zero or below 4e-15 square degrees. Standard startup/chunk/coverage/snapshot materializers rerun; ten focused stage/lake tests pass and all three required strict scenario contracts pass. No tolerance changed.

Final Pages package959.71MiB. Five serial tests against actual dist passed in1.9minutes: city lights33.0s, ocean geometry22.5s, Atlantropa coast18.6s, desktop/tablet commands5.2s and export preview17.2s. Root-owned localhost8000 server stopped after tests; existing user server retained. New city-light GeoJSON is pinned to LF because registry/manifest identities are byte-exact.

Remote PR132: first head62292f76 passed all three strict scenario checks and transport contract. Browser smoke stopped before any test: Playwright gitCommitInfoPlugin captured the entire large PR diff into one string, raising RangeError in spawnAsync before its later truncation. Set captureGitInfo.diff=false while retaining default commit/CI provenance and every test. Actual installed plugin setup under a GitHub PR environment passed locally with commit metadata and no diff attachment; all64 E2E tooling contracts pass. Remote checks must rerun on the fix commit.

Remote Pages mirror correction: incremental final scenario sync copied the full CATALOG.json but missed publication filtering, leaving only its recorded size different from canonical CI output. A complete fresh canonical Pages build succeeded; byte comparison across the full inventory found exactly app/data/CATALOG.json and pages-dist-manifest.json different. Adopted both; every tracked Pages file now exactly matches the full rebuild. Final total1006225011bytes (959.61MiB), exactly matching the remote canonical result. Product JS/CSS/geometry remain identical to the five-test browser run. Remote affected child-safe checks passed; only mirror step failed on head803f70b4.

Remote performance correction: head7c9e7bbc passed all required checks except perf-gate. Its same-runner five-sample refresh medians regressed from1865.1 to2221.2ms for TNO and2018.6 to2337.8ms for HOI4 1939, exceeding the unchanged1.15 ratio. Coastline source decisions were repeatedly decoding and measuring unchanged topology after overlay revisions, and static-mesh resets rebuilt identical coastline LODs. Cache geometric measurements by immutable topology/object/arcs/transform and measurement functions; reuse mesh LODs by selected geometry and simplification policy. Visibility still selects the correct source, and replaced geometry invalidates reuse. No performance thresholds changed.

Performance-fix validation:13 coastline/border behavior tests and all4 selected child-safe commands passed. Fresh canonical Pages build and byte comparison changed only the two renderer modules and manifest; total1006227790bytes (959.61MiB). Local interleaved startup samples suggest reduced repeated mesh work, but timings vary and do not establish gate admission. The first pixel probe mistakenly sampled inactive300x150 canvases; its pixel claim is discarded. The corrected probe uses the visible782x768 map-canvas after the standard performance settlement predicate, with nonblank checks: exact final pixel equality for TNO (3d192c4d) and HOI4 1939 (7baa5699), and identical scenario/land/water/coastline state. Remote CI on the new commit remains authoritative.

The focused actual-dist TNO coastline browser test also passed (22.7s), including submerged old coastlines and visibility-driven source/mesh changes.

September11 PR flow follow-up: user added6f26fbfe to the same integration branch to remove redundant CI setup and checks. The prior64dbeee3 performance run passed scenario refresh (TNO2284.5 to2141.7ms; HOI4 1939 2880.2 to2573.8ms), but failed HOI4 canonical render CPU at8914.5ms versus8880.3ms limit. This is not a passing performance result. The new workflow exposed an unmatched transport workflow path and a Golden Demo server-start failure immediately after smoke. Added the transport workflow to its existing structural-test route and assigned the Demo server port8811; smoke retains8810. Root owns the serial local smoke then Demo run, cwd repository, logs.runtime/tmp/daily-integration-20260911/pr-flow-{smoke,demo}.log; tests own and close their servers. All72 structural tooling tests and the520-route schema check passed. Product modules, data and Pages artifacts are unchanged by this CI repair.

Serial browser verification passed: four smoke tests in53.8s, then Golden Demo in55.4s using its separate port. Both commands exited0; no browser runtime failures. Required CI must rerun on the repaired workflow commit.
