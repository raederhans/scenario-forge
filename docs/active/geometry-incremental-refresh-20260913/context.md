# Context

Baseline source is current working tree; pre-existing WIP is national precision data/build work, not owned by this task. Main source js/ was initially clean.

CLI A task 0b3598e41eee4ecc8ba0627a84d3b562 owns political_collection_owner.js, projected_geometry_bounds_owner.js and their named tests.
CLI B task 4d29cf659135444d8c0804345dd1f95f owns geometry_raster_runtime_owner.js, geometry_raster_worker_client.js, geometry_raster_worker_kernel.js and their tests.
Both use claude-sonnet-4-6 through antigravity_agent, 2400-second task timeout, adapter checks quota before dispatch. Main independently accepts results. CLI agents may run short isolated Node tests only.

Main owns all shared refresh/index/color/signature edits and shared build/browser resources. Runtime outputs belong under .runtime/. No live runtime process started yet. Exact commands, port and logs will be recorded before those checks start.

Main server contract: cwd repository; hidden Python `tools/dev_server.py /app/ --port 8003`, MAPCREATOR_OPEN_BROWSER=0, MAPCREATOR_DEV_CACHE_MODE=revalidate-static, isolated MAPCREATOR_RUNTIME_ROOT=.runtime/tmp/geometry-refresh-server. Logs .runtime/tmp/geometry-refresh-server.stdout.log and .stderr.log. Success = owned listener and HTTP 200 source response; stop on failed bind/import; shut down own process after runtime checks. Existing port 8000 is unrelated and must not be stopped. Browser scope: one main-owned in-app tab localhost:8003, focused map zoom/chunk/edit/undo/scenario-switch, max 5 screenshots.

CLI A first attempt returned SUCCESS but no actual diff; resumed in same conversation with concrete implementation/API requirements. CLI B completed, parent added cross-surface union retention to prevent hit/political eviction thrash; 32 worker tests pass. Main index/delta/signature/refresh tests 60 pass; spatial/color/chunk Python boundary tests updated for new call shape and existing click-owner extraction, 52 pass. Architecture boundaries pass. Not yet browser-verified.

Investigation: saved de-benelux after-verified-perf sample has zoom-end primary refresh 976 ms, compose 191 ms for 12019 features. Historical sample only. Current code repeats geoArea, full clears geometry bounds and primary indexes, and uses topologyRevision in broad render signatures. Existing history is scoped; existing worker already supports geometry upload deltas but broad sceneKey resets storage.

Final integration: CLI A/B/C all completed; parent corrected same-feature geometry replacement, scalar property mutation and failed normalization retry in A; corrected political/hit union retention in B; accepted C's missing physical-mask, cities-marker, overlay-border resource dependencies after source review. Incremental baseline resets when no spatial build occurred. Semantic fallback can still reuse geometry bounds; scenario/projection change retains full reset. Reordering unchanged features reuses geometry with an updated draw-order item.

Validation: focused Node suites 139 cases plus 29 projection/deferred-infra/state-boundary cases passed; final affected scenario/index/signature set 61 passed and final cache suite 12 passed. Four Python contract modules 52 passed. Architecture, state-write allowlist, script portfolio and route schema passed; new cache/worker suites registered in local feedback catalog. No broad release/P4-history validation run.

Runtime evidence: .runtime/reports/generated/geometry-incremental-refresh-runtime.json. Final TNO 300% zoom then pan: 11968 spatial items reused, 221 rebuilt (12189 total), 214/12251 feature colors resolved; warm worker hit has 0 uploads and 0 path builds. Same-data incremental/full comparison: all 12251 colors, 12189 spatial items, picking keys and country indexes equal. Earlier integration click FR_ARR_87002 changed exactly one color; Undo restored every color. TNO -> HOI4 1939 settled with 11891 spatial features and zero index/feature mismatches; no captured console errors/warnings. These are focused local samples, not a controlled before/after speed benchmark; real primary refresh remained 766.7/899.1 ms.

Browser tabs closed; main-owned Python server 59072 on 8003 was verified by command line and stopped. Existing 8000 was untouched. Final artifact build runs in .runtime/reports/generated/geometry-refresh-pages-final (session 96838); initial build succeeded at 994.27 MiB, then final isolated build includes last null-feature guard/comment adjustment. Attempted cleanup of earlier own artifact was rejected by automatic policy; no alternate deletion attempted. Earlier artifact remains at .runtime/reports/generated/geometry-refresh-pages. Tracked dist and unrelated national data WIP remain untouched.

Completed: final build exited 0, 994.27 MiB; all 11 modified/new production source modules match the artifact with line-ending normalization. Three focused Pages manifest/module-graph/size contracts passed. Owned 8003 listener confirmed absent and tracked dist diff is empty. Current static delivery is below the 1 GiB hard gate but above its 950 MiB warning threshold; computation caching does not reduce this data footprint. Do not conflate this local completion with CI, committed delivery, or a global FPS improvement claim.
