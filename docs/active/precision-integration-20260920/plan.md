# Precision integration

Integrate the completed renderer/cache/worker optimizations, twelve-country European precision expansion and reviewed PL/UA seam repairs, SOV retirement, and full Russian replacement into protected origin/main. Preserve unrelated .playwright-mcp files. No history rewriting or protection bypass.

Sequence: compare inherited WIP and backup changed files; integrate the Russian worktree into the primary checkout; run relevant behavior/data checks and canonical Pages build; commit and open a PR; require protected checks and merge; refresh local main and remove only covered branches/worktrees after preserving runtime evidence.

Acceptance: all intended files included, source/dist consistent, required remote checks successful, merge confirmed, recoverable cleanup and registry matching actual Git state. Existing mask/global-geometry and non-comprehensive FPS limitations remain documented.
