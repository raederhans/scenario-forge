# Context

Root C:/Users/raede/Desktop/dev/mapcreator. Prior read-only investigation reproduced planar-to-great-circle drift in base and TNO final assets. TNO [80,-70] and [90,-75] erroneously filled by two water regions; base [90,-80] is physical land but marine_indian_ocean contains it. Base [0,89] is ocean but no named region. Existing TNO has a positive-winding special case for south_indian_antarctic_ocean. Fix edge meaning and winding together.

Pre-existing WIP includes city lights, Atlantropa builder/coastline/quality, scenario contracts and startup builders, data catalog/registry/ledger, landing and dist. Preserve all; use surgical edits to shared files. Existing Atlantropa task remains active with its own staging and owner. Do not execute its rebuild or promote its candidates.

Ownership: water_compiler agent owns new map_builder/geo/water_geometry.py and tests/test_water_geometry.py. water_validation owns new tools/check_water_geometry.mjs and tests/water_geometry_runtime.test.mjs. Root owns integration, generated assets, runtime changes, shared process execution, and task records. Children may only run isolated short tests; root alone runs builds/browser/contract materializers.

Runtime output root: .runtime/reports/generated/ocean-geometry-20260911 and .runtime/tmp/ocean-geometry-20260911. No live process started yet. Before a shared build, record exact command, log and stop criterion here. py -3 -B has geopandas 1.1.3, shapely 2.1.2, topojson 1.10.

Root owns first staged rebuild: py -3 -B -u -m tools.rebuild_water_geometry --stage-root .runtime/tmp/ocean-geometry-20260911/stage1 ; cwd repository; log .runtime/reports/generated/ocean-geometry-20260911/build1.log; success is exit0 plus outputs.json and foreign topology decoded equality; stop on source/geometry validation error. No canonical data writes in this process.

Build1 failed before writes: quantized ocean mask contains invalid collapsed rings. Authority adapter now repairs a private planar mask with make_valid, retaining all physical topology objects unchanged. Root retry same command at stage1 (not created), log build2.log.

Build2 failed before writes on a collapsed quantized short arc. Compiler agent fixed endpoint preservation and bounded snap rounding; real base probe valid after actual decode. Root starts build3 at stage1, same command, log build3.log; stop on error, no canonical promotion.

Build3 stage1 completed without canonical writes. Full final validator found missing Arctic cap from treating spherical physical ocean as planar, and 1929 tiny TNO polygon components reversed by Python TopoJSON despite correct source winding. Root added final exported-asset Node gate, complete builder final-mask alignment, final-decoded source exports and staged input identity checks. Compiler agent owns spherical physical-mask adapter and encoder fix; validation agent owns regression/E2E. Current14 root target tests pass before adapter changes. Stage1 is diagnostic only and must not be promoted.

Root starts build4: py -3 -B -u -m tools.rebuild_water_geometry --stage-root .runtime/tmp/ocean-geometry-20260911/stage2 ; log build4.log. Includes physical spherical adapter, redundant winding disabled, recorded numeric component floor, final all-feature Node gate, unchanged nonwater proof and input identity. Stop on any geometry/contract error; no canonical writes.

Build4 stage2 base passed all71 and explicit polar/dateline profile. Detail failed on a post-snap densification point just off-grid. Helper now final-snaps before topology node construction and does not resample already-short quantized edges;18helper tests and fresh detail probe passed. Root starts build5 at new stage3, log build5.log, same stop/promotion criteria.

Build5 stage3 blocked by real mixed-ring TopoJSON reordering beyond its disabled input rewind; final encoded-ring planar direction restoration now passes actual3351-part westernIndian probe with0complements. Detail physical layers found regional-only and reachable via single na_v2 mode; scope explicitly expanded to inherit primary land/ocean exactly while retaining allotherdecodedobjects. Inherit helper actual8object JS equality passed; unquantized detail grows19.3MB to42.8MB beforewaterreplacement. Root starts build6 stage4 with all fixes and failedcandidate retention beforegate, log build6.log. Source/runtime/chunk geometry must match afterstandardmaterialization.

Stage4 all3 final profile gates pass: base71/detail71/TNO141. All5 canonical water files adopted with input identity checks and baselines.runtime/tmp/ocean-geometry-20260911/baseline. Detail land/ocean exactlyinheritprimary; otherdecodedobjects unchanged, including concurrently completedAtlantropa. Windows mapped-file truncation error onTNO runtime handledby atomicreplace; originalhash verifiedbefore retry, no data loss. Data manifest2topologyentriesrefreshed. Root runs py -3 -B tools/check_scenario_contracts.py --strict --write-safe --report-path .runtime/reports/generated/ocean-geometry-20260911/scenario-contracts.json, log scenario-contracts.log. This rebuildsdependentstartup/chunks/allbasehashmetadata; stop onerrors, do notrelaxpolicies.

Final existing TNO shape and publish checks found5subpixel selfintersections, plus1 in unquantized NA detail. Compiler shared1e-9degree numericgrid repair underway; exactdecoded Shapelygate now precedesD3. Detail repair passed71features and foreignobjectequality, adoptedatomically;manifest timestamp unchanged for samebuild. Full6scenario strict repaired4successfully; blank_base ownerlesspolicy preexisting conflicts retained, onlysourcefingerprint/snapshot/audit synced. TNO needs repair+rerun. Root starts focused source Playwright onownedport8814, logbrowser-source.log, output.runtime/tests/playwright/ocean-geometry-20260911; exploratoryruntimeevidence whileTNOcandidate stillstaged, rerunfinal ifgeometrychanges. Stop ontestfailure; do notexpandallowlists.

Numeric repair canonicalTNO adopted after exactShapely+D3 pass. TNO focused18tests pass; fullfinalNode10tests pass incl9chunks exactsource/runtime and polar probes. Root targetedTNOstrict write-safe exit0. Root builds isolated Pages artifact with py -3 -B tools/build_pages_dist.py --output-root .runtime/tmp/ocean-geometry-20260911/pages-final ; log pages-build.log; stop on actualasset/sizegate, neverexpandlimit. Root onlysyncs water-dependent data outputs totrackeddist after validation; preserveotherJS/UIWIP.

Final closeout: compact water writer test passed, strictTNO regeneration idempotenttrue, all9chunk parsedpayloads identicalbefore/after75,511,492byte reduction. Isolatedpages-compact build959.07MiB passed;35 scopeddata mirrors synced, tracked947.43MiB passed. SourcefocusedE2E28.4s and tracked-dist26.9s passedrealclick/paint/undo. Tests usefull editmode, actualrendererprojection + SVGscreenCTM (container border is1px offset), nonemptyphysicalmaskfallback, and same selected/hover baseline forundo. Finalbrowseroutputisolated under.runtime/browser/ocean-geometry-20260911 toavoid concurrentdefaultPlaywrightroot cleanup. Distserver24980 port8815 stopped aftertest; rootsharedworkcomplete. New5geospatial testfiles registeredinheavy_dependency_groups;check_min_ci_requirements passed. Results and knownunrelatedblank_base/landing failures inresults.md.
