# Task

维护 `lessons learned.md` 与 `docs/active` 留档。

范围：
- 合并 `lessons learned.md` 中重复或高度重叠的条目。
- 移除已经明确解决、长期复用价值很低的 lessons 条目。
- 审计 `docs/active`，把已经完全完成且过于久远的任务归档到 `docs/archive`。

约束：
- 只做最小必要改动。
- 不改 README。
- 主线程独占任何验证与最终归档动作。
