# Migration performance and project adaptation follow-up

User authorized continuing remaining migration, performance validation and
optimization, and scenario/project adaptation after PR #146 reached 18 green
checks on `a95f2f0a`.

Working target: reduce the actual migrated coarse payload and editing cost while
preserving runtime geometry, historical assignments, old-project imports and
arbitrary whole-shard mixed-LOD seams. No production publication is authorized.

Root owns `tools/scenario_chunk_assets.py`, targeted LOD tests, all full builds,
localhost preview/browser operations, CI and integration. Root commands run in
the main checkout, with new outputs under
`.runtime/{tmp,reports/generated}/global-migration-followup-20260923/`.
Old candidates under `global-migration-20260923` remain immutable references.
Root is the sole owner of the partial-coverage diagnostic script and its output;
it may inspect existing detail chunks and write a report, never scenario data.

Independent work:

- Native `project_adaptation`: old-project planner and relevant tests only;
  isolated synthetic Node tests allowed, no full builders or browser.
- Native `europe_sources`: feasibility followed by a bounded geometry-only
  153-ID European candidate under the task runtime directory; no full asset
  build or product edits.
- CLI task `13a627e46a3b42279dbaa1d8359d0b50`: resumed on Gemini 3.8 Flash medium,
  read-only; inspect partition/encoding contracts and reusable noding operations.

Existing evidence: combined TNO has 14,650 individually valid precision features
but invalid global coverage; only 42/141 precision shards and 2/50 USA shards
have valid coverage. This does not establish whether mismatch is inherited
overlap, missing noding or floating-point residuals. Candidate performance is
not accepted. Preserve all failed-candidate evidence and pre-existing
`.playwright-mcp/` content.

First experiment: determine whether valid, edge-matched subsets of an actual
load shard can simplify only their internal edges while retaining all rejected
features exactly and proving unchanged subset/shard union. Shared-boundary
repair and coordinate rounding remain separate, unapproved implementation ideas.

## Implementation and evidence

Project migration now advances the returned project's baseline identity only
after all feature-reference migrations succeed. The 28 planner/import-transaction
tests pass, including confirmed preflight, actual patch staging/application,
export identity and atomic rejection. Scenario-resource preparation is mocked
in that integration test; it does not prove browser undo or real scenario commit.
The existing baseline mismatch confirmation remains in place.

Regional coarse simplification isolates invalid-coverage participants globally,
then simplifies valid subsets inside actual load shards with exact subset and
whole-shard union checks. Inherited overlaps and failed shards retain original
geometry. Native review reproduced a containment case where GEOS only marks
the inner polygon; the implementation now also protects its interior-overlap
neighbours using a spatial index. The superseded first build was stopped; its
partial staging directory is not an accepted candidate. The corrected geometry
and chunk suites pass 25 tests, including containment, foreign overlap and
shard-union rejection.
France retains its existing all-or-nothing path. Diagnostics distinguish eligible
and retained counts; eligibility is not a claim of coordinate reduction.

The v2 complete candidate passed strict contracts but failed the wider geometry
gate: all 141 precision-shard unions matched, all 9,595 inherited precision
overlap pairs stayed equal and no new invalid edges appeared, yet intersections
with undeclared foreign features changed (including French, Chinese, Mexican,
Canadian and retained historical surfaces). It is rejected. The partial path
now also protects every precision feature with an interior overlap against the
nonregional source features. Boundary-only contacts do not trigger protection.

The CLI identified independently partitioned parent borders as a plausible
missing-node mechanism, but overstated this as the measured root cause of all
coverage failures. Parent acceptance uses measured geometry checks instead;
the CLI root-cause claim is not accepted as evidence.

The European geometry-only candidate includes BG21, CH11, FI16, HR8, HU13, NO9,
RO32, RS16, SE20 and SI7. The source, baseline and candidate selected coverages
are valid; all 11 selected owner domains and the total target surface have zero
area difference. All 21,050 untouched political geometries match exactly.
Complete stage, strict contracts, mixed-LOD and visual checks are separate gates.
Their latest receipts are recorded on PR #146; the geometry-only result does not
grant release approval. The full European build uses cProfile to locate build
costs; profiled elapsed time must not be treated as an uninstrumented benchmark.

## Accepted optimization candidate (v3)

`optimized-v3/tno_1962` passes strict scenario contracts and the expanded LOD
check. All 141 precision-shard unions match exactly. All 6,650 original invalid
coverage participants retain their geometry, with zero new invalid participants.
All 9,595 inherited precision-overlap intersections remain equal; the 97 foreign
intersection changes detected in rejected v2 are now zero. Runtime political IDs,
detail geometry and assignments are preserved; this is coarse-display optimization.

| Coarse payload | Before | v3 |
| --- | ---: | ---: |
| Raw JSON bytes | 123,051,048 | 103,363,110 |
| Gzip level 6 bytes, same local compressor | 37,151,933 | 30,017,557 |

This is a 16.0% raw and 19.2% compressed reduction. Do not compare the earlier
stored gzip level 9 receipt with these gzip level 6 measurements. The build
records 446.657 seconds and 3,886,755,840 peak working-set bytes. It is not a
controlled build-time comparison; build performance remains costly. The cache
report selects all 239 political detail chunks and 16 context chunks for reuse;
actual stage timings, not a presumed cache miss, must guide further optimization.

Receipts under `.runtime/reports/generated/global-migration-followup-20260923/`:
`optimized-v3.strict.json`, `optimized-v3-lod.json`, and
`optimized-v3/tno_1962.build-report.json`. The wider LOD check also explicitly
compares precision intersections with undeclared foreign detail features.

The fixed-projection microbenchmark uses the repository's D3 Equal Earth path
generator, a canvas-command counting sink, two warmup pairs and five alternating
measurement pairs. Both variants contain 21,203 features. Median parsing time
is 1,171.81 -> 989.48 ms; median projected-command generation is
15,090.86 -> 11,653.61 ms; commands are 20,280,504 -> 15,482,136. This measures
neither canvas rasterization nor browser frame rate/network/scheduling.
See `coarse-benchmark.json` for every sample and the exact projection.

Localhost baseline/v3 browser observations reach an unlocked editor and support
zoom in/out; no HTTP 4xx/5xx is present in either task preview log. The single
zoom observations show political passes around 229.5 and 213.1 ms. Startup/last
frame timings cover different invalidated passes and are not comparable timing
gates. `browser-perf.json` preserves the raw visible overlays; visual acceptance,
statistical interactive performance and real-project browser undo remain open.
Both task previews/pages were closed; the user's original dev server remains.
Pages distribution rebuilt successfully at 550.37 MiB.

## European stage and main integration

The 153-target full European stage passes strict contracts and 24 regional
mixed-LOD combinations, with zero selected surface delta. Its coarse payload is
105,006,413 raw bytes / 30,578,911 gzip-6 bytes. Full visual acceptance remains
open. Receipts: `europe.strict.json` and `europe-partial-stage/validation.json`.
The profiled full process takes 536.6 seconds; recorded build/finalize scope is
465.391 seconds with 3,918,671,872 peak working-set bytes. Profiling affects
timing. It exposes 70.1 seconds in the two `sizes()` calls, which redundantly
compress political chunks for totals and largest-chunk rows.

While this work ran, main advanced to `638ee531` through showcase/export PRs
#147/#148. PR #146 could not start new checks because the generated Pages
manifest conflicted. Preserve both source changes, regenerate that manifest,
and retain the other worktrees. After integration, all 29 project planner/import
transaction tests pass, including main's staged scenario-color preservation.
The integrated Pages build is 570.20 MiB; the increase includes upstream
showcase assets, not publication of the local migration candidates.
