# Context

## 2026-09-23 active continuation

- Current results and blockers are in `continuation-20260923.md`; older 47-ID Japan evidence is superseded by the current no-nearest gate (5 fresh targets, 42 preserved). First 241-target Europe stage is rejected; AL/MK 20 geometry gate passes. Never promote an old ignored candidate on the strength of older prose alone.
- Root rebuilt Modern US and passed strict/20-shard LOD gates. HGO, freshly rebound blank and TNO complete historical stages pass strict contracts. Combined JP5/CN254/AL12/MK8 stage passes strict and 38 mixed-LOD checks, and reached unlocked idle editor in localhost smoke. HOI4 full stages are the remaining serial resource steps. Owned browser and preview port 8013 are closed; the pre-existing port 8002 dev server was not touched.
- CLI startup fixture and Modern observation support completed, with parent 11/68-test reruns; Air inventory was cancelled after invalid output, then parent produced the inventory. Borrowed-effect exact source receipts were refreshed only after semantic review; focused 61 and adjacent 69 tests pass.
- Read-only CLI task `13a627e46a3b42279dbaa1d8359d0b50` completed on Gemini 3.8 Flash medium. Parent verified the coarse simplifier's all-or-nothing fallback and manifest flags; exact trigger and savings are unmeasured. Do not treat the suggested per-shard optimization or coordinate quantization as accepted implementation. Combined coarse raw/gzip is 123,051,048 / 37,034,214 bytes and performance acceptance remains open.
- Draft PR: https://github.com/raederhans/scenario-forge/pull/146. Six strict scenario lanes, smoke, quick-fill and governed performance gate pass on `4d576500`; fast CI exposed an ignored CZ/SK cache dependency in a test. The test now builds its own provenance-bound fixture, and the parent assembler/source suite passes 20 tests. Latest-head CI must be checked again after push.
- Root works in `C:/Users/raede/Desktop/dev/mapcreator`, branch `codex/global-data-migration-20260923`, baseline `7d875e7c109187c4eeb1345ce9bfa09575635597`. Pre-existing untracked `.playwright-mcp/` is protected. The retained 20260922 worktree is read-only input; its canonical files and ignored evidence must remain intact.
- User requested CI and subagent continuation for remaining continental Europe, CN/JP/US and the existing IN template; CA/MX are optional light assessment. No automatic production deployment or threshold relaxation.
- Read-only delegates: country_inventory (country/source completeness), bundle_path (historical bundle contracts), perf_ci_diagnosis (current Nightly failures and actual runtime profiling surfaces). No delegate starts a shared builder, server or browser.
- Root owns CI scenario coverage, task records and later serial build/browser processes. Commands run from this checkout; reports/logs under `.runtime/reports/generated/global-migration-20260923/` and `.runtime/tmp/global-migration-20260923/`. Stop a candidate on domain/reference/strict-contract failure; publish a stage only after full assembly succeeds. Resource costs are measured independently from FPS claims.
- Current GitHub main deploy and perf gate succeeded. Nightly run `35785752495` failed; exact job-level findings are under investigation, not dismissed as migration-independent.
- Root serial process commands: `python -u tools/prepare_us_county_seams.py --county-source C:/Users/raede/.codex/worktrees/precision-expansion-20260922/mapcreator/.runtime/tmp/us-county-upgrade/source-national-verified/counties.geojson --baseline-runtime data/scenarios/modern_world/runtime_topology.topo.json --output-dir .runtime/tmp/global-migration-20260923/us-source-seams` completed successfully (3144 counties; coverage valid). Then `python -u tools/stage_us_county_scenario.py --county-source .runtime/tmp/global-migration-20260923/us-source-seams/counties.geojson --output-dir .runtime/reports/generated/global-migration-20260923/modern/modern_world`, session 94941, log `modern-build.log` under the task temp directory. No other full builder may run until this one exits.
- Original modern v4 source binding differs from current checkout only in CRLF/LF bytes; parsed baseline JSON is identical. New source processing binds actual current bytes instead of changing the old report. Other five historical runtime baseline files match the retained checkout byte-for-byte.
- CLI tasks: `0398f0e253bf4bea98ab7282573987fc` Air country extraction; `7f6635fb203e4214a1ba3742cfc0db96` Gemini startup fixture (completed; root reran 11 tests); `a604519e1e1044b2b5f91953beced6b1` Gemini explicit modern performance observation support. All use workspace_write with disjoint owned files; no CLI owns a live browser or real bundle build.
- Native historical_bundle owns new stage assembler and synthetic tests; europe_sources owns selectable cached-source preparation; policy_ci_fix completed narrowed hierarchy inputs plus source-bound policy metadata (69+30 targeted tests). Root owns final integration and dist sync.

## Latest: seams and project migration continuation

- Modern candidate is now `.runtime/reports/generated/us-county-national-v4/modern_world`; source `.runtime/tmp/us-county-upgrade/source-seams-v1/counties.geojson`. Explicit policy preserves existing neighbor domains, does not establish international/historical authority, and fills no gaps. 41 foreign features / 60 changed counties; 3144 counties retained. Removed union area 0.044762809868779105 deg2. Source union has zero foreign area; rebuilt runtime reports 10 individual floating-point residues, largest 3.571666609925436e-15 deg2, all below disclosed 1e-12 deg2 bound. Do not describe rebuilt geometry as bit-exact zero overlap.
- Full source/detail and arbitrary whole-shard LOD gate PASS (20 shards); strict scenario contracts PASS. V4 preservation check: all 10804 non-US geometries/properties/owners/cores identical to canonical, India718 untouched, city/capital sidecars byte-identical. Build94.531s, peak1463455744B, US detail gzip9950470B.
- New explicit project migration planner is connected to import preflight. Candidate changes stale baseline hash to stable_json_hash(new owners), maps481 stable county identities, and rejects edits to8 oldzones + invalid US_CNTY_09120. Area crosswalk remains evidence only. One-to-many membership/paint support, collision/default-vs-edit checks, one-to-one spatial anchors and story focus, and special-zone fingerprints are covered. No mutation before failed preflight; export keeps target baseline via existing scenario commit.
- Final Python target checks46 +4subtests passed; Node migration/roundtrip/transaction68 passed. Transaction tests reused existing exact acorn8.17.0 from main through a runtime-only Node loader; no install/package change. Actual v4 manifest test:481 stable identities and9 explicit rejection cases passed.
- V4 browser smoke: Modern World applied, Twin Falls County Idaho fill/undo at100%, Graham County Arizona fill/undo at144%, Undo disabled after restoration, US detail shards HTTP200, owned preview log no404/500. Evidence `.runtime/browser/us-county-seams-v4/`. Root tab closed and serverPID34424 stopped. Main only pre-existing .playwright-mcp/. No canonical replacement, commit, push or release.
- Historical final v4 patches completed: blank/modern domain alternative489/490 (CT invalid retained), HGO748/748, HOI4 each911/914 (two thin-face domains plus Michigan topology conflict retained), TNO948/950 (two thin-face domains retained). All new assignment sidecars independently verified against old values. Known sidecar plans completed at us-county-scenario-sidecars-v1:6/6 pass, zero unsupported refs; HOI4 each40 VP hosts remapped with unchanged1500/1501 totals and identical buckets/metrics/resource points; TNO historical labels inherited. These are patches/plans, not complete historical runtime bundles. See major-countries-results.md for exact exceptions and remaining integration gates. All processes/agents are terminal; no preview tabs/servers remain.

## US county continuation — 2026-09-22

- CLI parallel continuation authorized: merged-group owner e8260903474343ddb65b5aef22cc39d1 (renderer owner/tests), startup read-only 3834d1166d1b4bbbaef74b3f49684fe1, inventory e9b8c98d9cd4457c8138619f2fc6cd07 (report only), shard-boundary builder 79c8088472e240f7987f3a283e354394 (scenario_chunk_assets.py and test_precision_shard_lod.py). Root owns validate_us_county_lod.py/test_us_county_lod.py, map_renderer diagnostics, documents and all national stage/browser processes.
- Proven additional bug fixed: all 20 US detail-shard domains previously differed between coarse and detail despite both full coverages being valid. Baseline gate .runtime/reports/generated/us-county-lod-before.json is FAIL. Root completed shared detail partition planning before precision coarse simplification, strengthened reusable shard membership, and added source-bound mixed-LOD validator. New .runtime/reports/generated/us-county-lod-after.json is PASS: 20/20 shards, exact zero domain difference, valid source/coarse/detail coverage. This proves arbitrary whole-shard mixtures, not single-feature replacement or foreign seams.
- Current candidate is .runtime/reports/generated/us-county-national-v3/modern_world. Build completed in 112.328 s, peak working set 1,458,216,960 B. Strict scenario contracts PASS. v2 -> v3 runtime/bootstrap, owners/cores, city/capital sidecars and ALL detail chunks unchanged; non-US coarse including India unchanged. Only US coarse geometries changed. Global coarse gzip 9,251,657 -> 9,429,485 B (+177,828 B, 1.9%). Preservation report .runtime/reports/generated/us-county-v3-preservation.json. US 3,144 and India 718 remain; release_ready:false.
- Final v3 browser smoke passed: ready, world-view county fill/undo, zoom 144%, US detail chunks HTTP 200, Dundy County Nebraska fill/undo at 144%, Undo disabled after restoration. Evidence .runtime/browser/us-county-merged/v3-final-dom.txt and v3-final-144.png; no preview HTTP 404/500. All current root-owned tabs and preview servers are closed/stopped (last PID 34924), builds/validators exited successfully; CLI tasks terminal, failed data delegate cancelled before parent takeover. Main still only pre-existing .playwright-mcp/. No commits, push or release.
- CLI results were reviewed, not blindly accepted. Merged-group implementation retained after root strengthened tests (32 owner tests, 4 boundary tests). Data CLI's unsupported completion claim was rejected, stopped, and root implemented builder/test changes; relevant final Python checks 32 tests + 539 subtests, plus 6 validator tests. Startup unwrap finding was retracted after checking the bridge; no bootstrap initialization or prewarm threshold changes. Air inventory is parent-corrected and not a release verdict.
- Root live validation contract: cwd this worktree; python -u .runtime/tmp/us-county-upgrade/preview_server.py 8002 .runtime/reports/generated/us-county-national-v2/modern_world; log .runtime/tmp/us-county-upgrade/preview-merged-followup.log. Root alone starts/polls/stops port 8002 and in-app browser tabs. Compare same staged dataset and settled frame, stop on correctness failure; all temporary resources closed at completion. Subsequent v3 candidate build and validation are root-only, output .runtime/reports/generated/us-county-national-v3/modern_world, log .runtime/tmp/us-county-upgrade/modern-national-v3-shard.log; no canonical publication.

- Performance continuation completed: retained full-pass Path2D references are reused across color regrouping with feature/geometry snapshots and scenario/data/projection/transform identity fences. No LRU budget or partial-repaint threshold changes. At the same national-candidate viewport, Nebraska 93-county fill settled frame 618.8 -> 134.3 ms; Undo 733.3 -> 264.0 ms; Redo 798.9 -> 271.0 ms. All three rebuilt zero paths and reused 13,947; these are individual local A/B samples, not a statistical FPS benchmark. Owner behavior 28/28 and boundary 4/4 passed. See performance-results.md and perf-followup-{baseline,after,pixel-comparison}.json under .runtime/tmp/us-county-upgrade.
- Root exclusively owned the follow-up preview server on localhost 8002 (PID 61208; preview-perf-followup.log) and the comparison browser tabs; all are now closed/stopped after command-line identity verification. No HTTP 404/500 in this preview log. Main still has only pre-existing untracked .playwright-mcp/. No data rebuild, canonical data change, or remote release in this performance continuation.

- Latest user decision: remove the historical ~900 US zone limit; retain India's existing district granularity and source. All implementation is in this isolated worktree; canonical data and main checkout are unchanged.
- Complete source: .runtime/tmp/us-county-upgrade/source-national-verified/counties.geojson and source-report.json; 3,144 counties/equivalents across 50 states + DC; raw 8,131,517 coordinates -> 1,111,124 display coordinates; native cross-state coverage valid. Original TIGER/Line 2025 archive retained and hashed. Display SHA256 d6b582b51055c3df715d21b1fc991d2de76de4d3798374c7bd92bde47c845a0f. The CB 2024 footprint is a cartographic mask, not a verified land-only or historical boundary source.
- Accepted data candidate: .runtime/reports/generated/us-county-national-v2/modern_world. Builder command python -u tools/stage_us_county_scenario.py --county-source .runtime/tmp/us-county-upgrade/source-national-verified/counties.geojson --output-dir .runtime/reports/generated/us-county-national-v2/modern_world; log .runtime/tmp/us-county-upgrade/modern-national-v4.log. Root session 5308 / PID 43204 completed, 113.516 seconds, peak working set 1,493,327,872 B. Source hash + complete per-state membership required; directory is published from an owned temporary sibling only after successful build.
- Bootstrap uses existing shell builder: 435 Russia shell helpers, 307,884 raw / 60,476 gzip bytes; it is not empty. The earlier proposed empty-shell strict-contract exception was withdrawn entirely. Original strict contracts pass, report .runtime/tmp/us-county-upgrade/national-contracts.json. Previous full-geometry 47,874,123-byte bootstrap is superseded. Startup locale/alias assets now both return HTTP 200.
- Independent data audit .runtime/reports/generated/us-county-national-audit.json: 10,804 non-US features exactly preserve decoded geometry, properties, owners and cores; India 718 unchanged; all 3,144 county IDs match source; 344 city/capital hosts valid. 20 US detail chunks: 10,031,108 gzip bytes total, largest 746,354; global coarse 9,251,657 gzip bytes. 41 foreign-boundary overlaps remain review findings. Old invalid Connecticut polygon US_CNTY_09120 is explicitly unresolved for legacy crosswalk; no invented repair or nearest assignment.
- Root browser check: localhost 8002 national candidate reached ready and visibly filled Chase County, Nebraska; Undo -> Redo -> Undo reflected real enabled/disabled history states. Evidence .runtime/tmp/us-county-upgrade/national-browser-edit.json. One visible fill action was 31.2 ms; this is not an FPS benchmark. No HTTP 404/500 in national preview log. Initial apply sample 2,325.1 ms; coarse rendering remains a performance target.
- Hierarchy adaptation complete: base Nebraska's six legacy children are replaced by the candidate's 93 counties. Stage manifest carries scoped hierarchy_overrides with 51 states / 3,144 county IDs. The pure cached resolver and five consumers preserve base hierarchy and use existing active-manifest rollback semantics. 28 related Node behavior tests passed; an actual source/manifest/resolver check proved exact 3,144-ID membership and non-US group reference preservation. Browser reload loaded the new module HTTP 200; State level, double-click fill, and one-step Undo passed. DOM evidence: national-browser-hierarchy.json.
- All root-owned source/build/audit processes, browser tabs and preview servers are now closed/stopped. Former baseline 8000 PID 19736, three-state 8001 PID 58740, national 8002 PID 54752 used .runtime/tmp/us-county-upgrade/preview_server.py with dedicated logs. No shared process was stopped.
- Current target checks: 11 source + 12 stage Python tests, 28 hierarchy/consumer Node tests, original strict scenario contracts, and diff whitespace check passed. Main checkout retains only the pre-existing untracked .playwright-mcp directory; canonical data and remote state unchanged.
- Measured remaining runtime bottleneck: at 1123x960 viewport, DPR 1.5 and world zoom, State double-click (Nebraska 93 counties) took 607.4 ms for the settled frame (political 585.7 ms) versus 29.3 ms edit logic. A bounded dirty prefilter experiment retaining existing candidate/coverage/cache gates did not improve the actual case (754.4 ms settled); its code/tests were withdrawn. Keep the evidence national-browser-partial-repaint.json as a rejected experiment, never report its earlier not-yet-painted 22 ms overlay as success. Next performance step is actual fallback-reason/background-cache profiling, without widening safety budgets.
- Remaining release conditions: foreign seams, old project split/merge edits, historical scenario cuts, broader visual/target-device performance. Candidate remains `release_ready:false`; do not replace TNO/HOI4 with modern counties without their separate adaptations.

## Current truth

- 当前 canonical TNO detail manifest 约含 12k feature；现有 chunk cache、geometry cache 和 load scheduler 已有明确的条目/字节/并发预算。
- 西欧精度工作改善了跨国边界，但东欧内部仍存在未分类的缝隙；现有 Russia pipeline 明确不承诺填补继承空洞。
- 中国、日本、印度、美国均已有可用来源或现有 detail 数据，但来源粒度、简化率、ID 映射和成本差异显著，不能用一个固定简化参数直接批量替换。
- 已有 LOD 候选工具、调度器、增量存储和紧凑 worker 几何基础，本轮应在现有契约上推进。

## Decisions and deviations

| Time | Evidence or decision | Impact |
| --- | --- | --- |
| 2026-09-22 | 用户授权 CLI 与子代理并行完成三条线 | 可并行推进，但主代理统一整合、验证与风险判断 |
| 2026-09-22 | 用户授权性能低风险和中风险两个阶段 | 两阶段可实施；高风险阶段只评估，不落地 |
| 2026-09-22 | 创建托管 worktree `precision-expansion-20260922` | 原主工作区和未归属 `.playwright-mcp/` 保持不动 |

## Live process ownership

| Process | Owner | Log path | State |
| --- | --- | --- | --- |
| Native agent: East Europe | `/root/east_europe_gaps` | agent handoff | complete; audit/candidate only, no canonical mutation |
| Native agent: major-country precision | `/root/major_country_precision` | agent handoff | complete; isolated candidate pipeline |
| Native agent: performance phases | `/root/performance_phases` | agent handoff | complete; low/medium implemented, high-risk analysis only |
| CLI read-only inventory (rejected) | router task `bcb8f1cd0ee14198af3ae8ad157dfdad` | CLI handoff | completed but failed evidence acceptance twice; approximate/hallucinated counts excluded from decisions |
| CLI exact inventory retry | router task `6f3c4e2bf022450eae01f0f290066e22` | CLI handoff | incomplete; backend exposed no execute/write tools, so it could not produce exact counts and was excluded from conclusions |
| TNO LOD candidate measurement | `/root/performance_phases` | `.runtime/reports/generated/performance-lod-20260922/` | exclusive builder ownership; FRA part.0, RKM part.5, NOV; no port or canonical writes |
| JP full-topology candidate assembler | `/root/major_country_precision` | `.runtime/reports/generated/major-country-precision-v3/` | complete; no live GIS process |
| JP staged scenario asset build v1 | `/root`, former PID 56624 | `.runtime/reports/generated/jp-precision-stage-v1/build.log` | exit 0 and full stage produced, but superseded before acceptance because independent review found mutable-`cntr_code` target selection |
| Corrected four-country candidate v4 | `/root`, former PID 32792 | `.runtime/reports/generated/major-country-precision-v4/build.log` | complete; JP ready; CN overlap, IN source, US lineage fail closed; no live GIS process |
| JP staged semantic/LOD validation | `/root`, former PID 60908 | `.runtime/reports/generated/jp-precision-stage-v1/validation.log` | complete; invariants pass, 47 targets and 4 mixed-LOD combinations pass |
| JP staged browser smoke | `/root`, former dev server PID 26672 and closed Playwright session `jp-stage` | `.runtime/browser/jp-precision-stage-v1/` | complete; staged JAP detail chunk returned 200, loaded 47/47 target IDs, console 0 error/0 warning, no failed request; temporary server and browser closed |

## Handoff

所有实现发生在 `C:/Users/raede/.codex/worktrees/precision-expansion-20260922/mapcreator`。不同执行者使用互斥文件所有权；共享 manifest、catalog 和 task record 由主代理整合。

## Next step

本轮授权范围完成。后续若继续数据接入，先解决 CN overlap、IN governed source、US lineage 与东欧 recipient/water 证据；若继续高风险性能实验，先在目标设备采集 trace/heap 基线。基线与本轮回归证据见 `task.md`。

## Continuation 2026-09-22

用户在关联任务中授权继续精度升级、性能优化和细节修复；本任务接手原隔离 worktree 的未提交修改。原主 checkout 不变。

- `/root/major_precision_continue` owns major-country candidate tool/tests; explicit governed source paths and bounded unchanged-domain upgrades.
- `/root/exact_lod_consumers` owns JS exact-geometry consumers and related tests; ID-only editing/save must not trigger unnecessary global detail loads.
- `/root/builder_memory` completed ragged arc decoding; parent owns full integration measurement.
- `/root` owns validator, source-gap follow-up, docs and integration. Validation now supports explicit stable target IDs and rejects surface/owner-domain drift.

Parent-only live build: cwd this worktree; command `python -X utf8 tools/build_tno_russia_precision_assets.py --baseline-dir data/scenarios/tno_1962 --candidate-runtime .runtime/reports/generated/major-country-precision-v4/output/jp.candidate.topo.json --output-dir .runtime/reports/generated/jp-precision-stage-v2/tno_1962`; output/log root `.runtime/reports/generated/jp-precision-stage-v2/`; no ports or canonical writes. Success is exit 0 with complete build report; stop on invariant failure, never weaken gates. This reruns the exact JP input with the new decoder to measure real build peak memory and compare assets to v1.

Parent-only browser server: `python -B -u .runtime/tmp/precision_stage_server.py .runtime/reports/generated/jp-precision-stage-v1/tno_1962`, cwd this worktree, localhost port 8000, logs `.runtime/browser/precision-continuation/server*.log`. Existing validated v1 stage serves against current JS until v2 completes. Stop after scoped browser export validation; no canonical writes.

JP v2 build complete: all generated JSON bytes equal v1; peak working set 17,400,205,312 -> 2,391,736,320 B; elapsed 292.688 -> 183.5 s (single runs). Current validator passed JP stage; four mixed-LOD combinations pass. Browser generated a valid 1418x2062 PNG through the real toolbar pipeline; native download notification timed out, so the generated data URL was captured temporarily and saved under `.runtime/browser/precision-continuation/map_snapshot.png`. Capture hook restored and working tab closed; 0 console warning/error. This legacy-manifest smoke does not establish the declared-family production gate.

Parent-only combined stage: command `python -X utf8 .runtime/tmp/compose_precision_stage.py`, cwd this worktree; fresh output `.runtime/reports/generated/precision-combined-v1/`, stdout/stderr `build*.log`. Inputs JP v4 and CN/IN/US v9; expected 367 selected IDs. One assembler/builder/validator owner; no canonical writes or ports. Success requires complete staged validation with stable IDs, owner/surface equality and mixed-LOD checks; failure remains a blocking finding rather than relaxed tolerances.

Combined v1 stopped by its owner before any candidate/stage output: `_assemble_candidate` was redundantly realigning already frozen same-baseline candidates over a near-global extent (344.52 CPU seconds, no completed output). v2 rebases/compacts candidate arcs without modifying coordinates, explicitly checks all target/non-target geometries against source/baseline and metadata, then uses the same formal staged builder (which recomputes adjacency) and strengthened validator. Same command, fresh output/log root `precision-combined-v2`. This avoids repeating spatial alignment; all acceptance gates remain enabled.

Combined v2 completed asset generation but FAILED mixed-LOD acceptance (`owner union mismatch for BRM`). Explicit geographic target IDs were assembled correctly, but metadata still added whole-country precision flags; this omitted reassigned targets from coarse preservation and expanded precision across untouched country units. The shared assembler now preserves baseline country flags and adds exact `political_precision_feature_ids` for the explicit-ID path. A regression covers reassigned targets and existing baseline flags/IDs; 22 regional/major-country/explicit-LOD tests passed. v2 is rejected and must not be promoted.

Parent-only corrected build: PID 28264, command `python -X utf8 .runtime/tmp/rebuild_precision_combined.py`, logs/output `.runtime/reports/generated/precision-combined-v3/`. It changes only precision metadata on the coordinate-verified v2 composition, adds all 367 selected IDs, rebuilds formal stage assets and runs the unchanged strengthened validator. No port or canonical writes. Previous browser server PID 27708 is stopped, working tabs closed; one earlier browser-created failed-navigation tab could not be explicitly closed because it is a policy-blocked data error page.

## Final continuation acceptance

Combined v3 build and validation completed successfully; PID 28264 exited. 367 selected IDs, 9,387 -> 33,450 coordinates, zero selected surface and owner-domain drift; all 42 mixed-LOD checks pass. 208 chunks, gzip total +898,324 B, JSON total +3,519,872 B; startup gzip sizes slightly decreased (25 B per language). Build elapsed 204.281 s and peak working set 2,399,203,328 B. `validation.json` remains `invariants_pass_visual_review_required`; full visual seams/target-device benchmarks are not claimed.

The continuation browser server initially omitted `/app/` normalization, so earlier PNG/family runs did not establish staged asset loading. Corrected server PID 33172 explicitly logged staged paths and HTTP 200 for coarse and Japan detail. This revealed a repeated startup first-frame race: main's synchronous assertion ran during layout interaction; the renderer later painted a valid frame but the error overlay remained. The support helper now waits for the existing render phase transition before flush/assert, with a finite active-frame deadline and no manufactured readiness. Six behavior and five boundary tests pass. Corrected browser reload: `bootPhase=ready`, empty error, first visible true, blocking false. Temporary family regional -> detail committed 47 JPN IDs / 12,428 coordinates with no pending promotion. Tab closed and PID 33172 stopped. This final probe supersedes early staged-browser claims. Main checkout still only contains its original untracked `.playwright-mcp/`; canonical data and remote state unchanged.

## County seams and migration continuation (root ownership)
- Root owns prepare_us_county_seams.py, stage_us_county_scenario.py, seam tests and docs. Native agents own project migration JS/tests and historical adapter/test respectively. CLI 54763acbcbe44cd1831e4f1831cbc62c is read-only sidecar inventory.
- Root-only commands, cwd this worktree: `python -u tools/prepare_us_county_seams.py --county-source .runtime/tmp/us-county-upgrade/source-national-verified/counties.geojson --baseline-runtime data/scenarios/modern_world/runtime_topology.topo.json --output-dir .runtime/tmp/us-county-upgrade/source-seams-v1`; then stage_us_county_scenario.py with that source into `.runtime/reports/generated/us-county-national-v4/modern_world`; then validate_us_county_lod.py into `us-county-lod-v4.json`. Logs `.runtime/tmp/us-county-upgrade/seams-v1.log`, `stage-seams-v4.log`, `lod-v4.log`. No ports. Success requires complete 3144 membership, valid coverage, zero positive-area foreign overlap and unchanged non-US geometry. Stop on geometry or assignment failure; never repair/snap/nearest to force a pass.
- Policy is a candidate display seam constrained by existing neighboring domains, not a new claim of international boundary authority. Historical adapter preserves scenario domains; modern source is never an ownership oracle.
- Root historical candidate command: `python -u tools/adapt_us_county_scenarios.py --county-source .runtime/tmp/us-county-upgrade/source-national-verified/counties.geojson --output-dir .runtime/reports/generated/us-county-scenario-adaptation-v1 --all-us`; root owns process and output, log `.runtime/tmp/us-county-upgrade/historical-overlay-v1.log`. No other writer or rerun. Success is explicit per-feature preservation/assignment checks and final six-scenario summary; unresolved features retained rather than patched. This is a patch candidate, not deployable bundles.
- Historical v1 strict equals rejected 924/950 TNO domains; measured Wayne had area and Hausdorff drift both zero. v2 uses disclosed 1e-12 deg2 area AND 1e-10 degree Hausdorff bounds without changing/removing geometry, recognizes HGO USA. Root reruns same command with output us-county-scenario-adaptation-v2 and log historical-overlay-v2.log; v1 retained as evidence, no concurrent historical builder.
- Final v4 root browser contract: `python -u .runtime/tmp/us-county-upgrade/preview_server.py 8002 .runtime/reports/generated/us-county-national-v4/modern_world`, cwd this worktree, log `.runtime/tmp/us-county-upgrade/preview-seams-v4.log`; root alone controls port/tab/process, close after modern ready + focused county edit/undo smoke, stop on HTTP failure. No external sites.
- Historical v3 uses joint boundary noding and polygonization after real Alaska/Florida/Kansas samples proved separately computed intersections created internal roundoff artifacts; thresholds unchanged. Root command same as v2 with output us-county-scenario-adaptation-v3 and log historical-overlay-v3.log. No v2 process remains active.
- Historical v4 final root command uses same all-us invocation with output us-county-scenario-adaptation-v4 and log historical-overlay-v4.log. New evidence HGO-S368 ambiguous representative point had zero-area intersection with both counties; classify boundary-only points by complete face geometry, preserving residual rather than choosing a county. Baseline decoder records strictly repeated-point zero-extent parts; no invalid polygon repair. V3 completed and remains available, no concurrent historical runs.
- Root sidecar follow-up: `python -u tools/prepare_us_county_adaptation_sidecars.py --adaptation-dir .runtime/reports/generated/us-county-scenario-adaptation-v4 --output-dir .runtime/reports/generated/us-county-scenario-sidecars-v1`, cwd this worktree; log `.runtime/tmp/us-county-upgrade/historical-sidecars-v1.log`. New output only, root owns execution. Success requires source/baseline binding, unchanged strategic buckets/metrics/resource points, supported refs resolved or entire affected file withheld; report is a sidecar plan, not bundle readiness.

## Integration authorization and live ownership (2026-09-22)
User authorized commit and merge of the accumulated implementation. Root is the sole Git integration owner. Fresh origin/main and both worktree HEADs were 382ce56744d3942a9097e1352c312a23bab0dbf9; main has only protected untracked .playwright-mcp/. Canonical data remains unchanged; all precision and county outputs remain ignored candidates under this worktree's .runtime. Retain this worktree after integration to preserve those candidates and unresolved historical-boundary evidence.

Root-only commands, cwd this worktree: `npm ci`; targeted Python and Node regression suites; `npm run verify:pr`; `python -X utf8 tools/build_pages_dist.py`; `python -X utf8 -m unittest tests.test_pages_dist_startup_shell -q`. Logs live under `.runtime/tmp/integration-20260922/` (npm-ci.log, targeted-python.log, targeted-node.log, verify-pr.log, pages-build.log, pages-test.log). Exact targeted test lists are recorded in run scripts there. No local browser server is planned; canonical builder exclusively owns dist. Success requires exit 0 and unchanged canonical data; stop and diagnose failures without relaxing gates. Root owns GitHub PR/check monitoring; read-only integration reviewer owns no processes or mutations. Required remote checks and the exact PR head must pass before merge; no historical performance waiver is reused.

- User additionally authorized GitHub Pages update. Root will follow the main-push deployment through completion and verify public asset identity; no unvalidated candidate data enters the release.
- Fresh integration targets: 82 Python tests and 121 Node tests passed. verify:pr exposed two integration issues: renderer owner line budget (1415 > 1380) and the legacy promotion-boundary count. Deduplicated identical renderer metrics/empty summaries within the existing injected owner (1379 lines, no changed guard or budget); 32 renderer behavior and 4 boundary tests passed. The exact export path is a fourth caller of the same guarded commit entrypoint; the boundary assertion now requires four and specifically checks its rethrowing export call, while retaining one direct commit call. All 38 chunk-refresh boundary tests passed.
- Canonical Pages builder rerun after the renderer deduplication; final logs pages-build-final.log/pages-test.log. Candidate datasets and canonical data are unchanged.
- Integration routing follow-up: registered nine new geospatial tests in heavy_dependency_groups.json and matched unittest/pytest entrypoints to repository discovery. Classification and the previously failing route inventory test pass. The prior full PR command passed its architecture, E2E/console/timeout guardrails, 166 boundary/structural tests and strict TNO contracts; its adaptive phase is rerun separately after the metadata correction to avoid repeating unaffected gates.
- Final adaptive phase completed with exit 0: 125/125 commands passed; 88 main-thread commands explicitly deferred. This does not claim the deferred browser/performance commands ran locally; required PR workflows own remote smoke/contracts/performance acceptance. Together with the already passed pre-adaptive gates, local PR prerequisites are complete. All root local build/test processes are terminal.
- Implementation commits: f6c4ad98f1c45dff94fb6d59a25d2f26957040f1 (runtime/Pages), 4cc231583b3cdbc16c7f7008f80426fbe8692e9b (precision tools/tests/routing). Root retains this worktree for ignored candidates; no data or unrelated main-worktree content is staged.
- PR #144 first remote run: strict 1936/1939/TNO and transport contracts plus browser smoke passed. Quick Fill rejected direct singleton writes in the new hierarchy test. Replaced them with source-bound consumer functions using a local fixture (existing AST harness pattern), retaining the same 51-state/3144-ID, India, preset and clear-state assertions. Four hierarchy tests and the unchanged state-write allowlist pass. No runtime or Pages bytes changed in this correction; remote checks must rerun on its new head.
