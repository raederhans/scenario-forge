# Execution context

2026-09-20: current checkout contains extensive pre-existing precision-performance renderer/worker changes and tests, plus docs/active/precision-performance-20260920 and .playwright-mcp. Preserve all. Existing regional pilot uses GISCO 2021 01M shared source, retained scenario IDs, historical masks and outer-boundary reconciliation. FR is protected separately; baseline precision declarations are BE/DE/NL.

Target feature counts: GB173 LU3 IT93 AT35 PL378 UA495 BY34 EE5 LV6 LT10 ES50 PT23. NUTS source uses UK while scenario uses GB. LU uses Natural Earth ADM1 IDs. PL uses powiat IDs; BY hybrid aggregates require explicit lineage.

CLI router tasks: 6edab7a59a634ad6a09d51e61213f334 (Gemini 3.1 Pro high, read-only pipeline audit); 69747aaaa192412eacb3a65b01164570 (Sonnet 4.6, eastern source design). Eastern workspace_write setup failed before changes due missing command_status/send_command_input registration; resumed with read_only fresh session. Parent owns implementation.

Live resource owner: parent exclusively owns candidate generation and asset builds, serially, cwd repository root. Output/logs under .runtime/tmp/europe-precision-20260920/. No agent may run shared builds/browser. Each command will be recorded here before execution; success requires exit 0 and output checks. Failures preserve logs/candidates, no blind retries or gate relaxation.

User clarified visual acceptance: preserve TNO historical borders and improve fit; small deviations acceptable. Raw pooled multi-source coverage conflicts at PL/LT/UA. Existing baseline target coverage also invalid from prior generalized/quantized geometry. Begin NUTS8 joint candidate with existing strict pipeline; measure actual owner/feature drift before integration. Parent command: py tools/pilot_tno_regional_precision.py --scenario-dir data/scenarios/tno_1962 --replacement-geojson .runtime/tmp/europe-precision-20260920/source/nuts.geojson --source-countries GB IT AT EE LV LT ES PT --output .runtime/tmp/europe-precision-20260920/nuts-candidate.topo.json. Log nuts-candidate.log; only exit 0 plus validation permits continuation.

First NUTS8 build rejected ES612 area conservation residual 2.688907999464436e-12 deg2. Added opt-in numerical residual band (EPSG4326 only, capped 1e-9 degree, default remains strict), with unchanged raw residual reporting and no coordinate modification. Tests reject interior holes, visible shifts, invalid/broader tolerances and projected CRS. Retry same candidate command adding --boundary-tolerance 1e-9, separate nuts-candidate-band.log.

NUTS8 candidate passed:395 features,10254->147821 coordinates; ES612 residual confirmed within1e-9 degree band, no protected overlap. Lost2 Italy/SanMarino adjacency edges require visual/gap review. Parent next serial command py .runtime/tmp/europe-precision-20260920/extend_candidates.py LU PL UA; log extend-candidates.log; outputs separate LU/PL/UA candidates, reports include elapsed/peak working set.

Parent owns dev server py -u tools/dev_server.py --port 8004, MAPCREATOR_OPEN_BROWSER=0, log server.log, session83707. Browser skill used for actual local UI; quick scope focus4 regions/max5 screenshots per pass. CLI resumed workspace writes after user repaired registry: same task69747 east sources,6edab7 validation (new owned tools/validate_tno_precision_expansion.py and tests/test_tno_precision_expansion_validation.py only). LU/PL/UA serial build passed18.96/26.51/48.96s; peak process working set1.215GB. No production writes yet.

Visual-risk check found San Marino enclosing hole gap rose0.002587->0.003315 deg2 while protected SM polygons unchanged. Added explicit reviewed-enclave anchors to alignment: retain original baseline coverage within source holes intersecting requested non-target enclave SM; other source holes remain protected. Regression checks enclave ring retained and unrelated lake hole untouched. Rebuild NUTS8 from original baseline using --retain-enclave-countries SM into nuts-enclave-candidate.topo.json, log nuts-enclave.log, then replay LU/PL/UA. No production data writes.

NUTS enclave rebuild completed. Parent now runs py .runtime/tmp/europe-precision-20260920/extend_candidates.py --baseline .runtime/tmp/europe-precision-20260920/nuts-enclave-candidate.topo.json --prefix v2- LU PL UA BY, log extend-v2.log. BY source from CLI first valid output, compare against strengthened provenance-aware eastern-v2 before accepting.

Final runtime v2-by candidate built successfully, 1305 targets and 251093 coordinates (baseline40092). Eastern v3 source geometries verified equal by ID to those used. Both Gemini CLI delegates finished source/validator implementations; parent corrected final validation for legal chunk repartition, ring orientation and stale compressed siblings. All12 individual country coverages valid. Auxiliary6 and10714 untouched geometries exact;30 mixed-owner LOD combinations pass. SM two lost adjacencies restored.

Asset stage: .runtime/tmp/europe-precision-20260920/stage/tno_1962. regional_scenario_assets build exited0, observed peak working set11053293568bytes; existing builder partitions FRA/GER/USA,189->198chunks. Finalize via apply_safe_scenario_contract_repairs(rebuild_chunk_assets=False); strict scenario contract passed. Must refresh stale JSON.gz after finalization; validator now checks all stage compressed JSON siblings.

Candidate read-only server8005 maps BOTH /data/scenarios/tno_1962/ and /app/data/scenarios/tno_1962/ tostage. Initial browser samples used baseline due missing /app alias and must NOT support final conclusions. Corrected mapping, fresh load and actual state.activeScenarioManifest hash3ef4211f63a53c716ffe9f475556411e9721f5aa84f5052084a446e8100c0c3f verified. loaded-candidate.json records actual full12countries with substantially more coordinates. Final performance and fill/undo currently being repeated against corrected page. Baseline pose k5,x-2887.318740174423,y-1519.3701700091442,size1336x1200. Parent owns both server processes and browser tabs; viewport override must reset on cleanup. No production integration yet.

Later state: 39 staged files locally integrated after full baseline backup under .runtime/tmp/europe-precision-20260920/baseline/tno_1962. Owners/cores/countries byte-unchanged. Catalog regenerated with no catalog diff; data_health showed only large-file warnings;18catalog tests passed. Final compressed startup binding required rerunning safe snapshot/audit finalization after gzip refresh. Final strict contract check recorded separately as contract-integrated.json.

Actual candidate click/undo evidence: GB UKJ13,PL PL_POW_1418,ES ES425,IT ITI18,SM SM_ADM1_SMR-4888,all restored. Actual candidate perf31warm zoom render samples median4.8ms,max61.3ms, but candidate fit projection differs from baseline; NOT a controlled geographic before/after benchmark. Initial stale-candidate metrics are invalid and superseded.

User then explicitly requested inspection of obvious TNO Ukraine/General Government seams. Read-only seam diagnosis found inherited source gaps; local trial integration remains but visual acceptance not granted. Fixed window uncoveredarea2524.44->1724.24km²;largest two current gaps788.05/468.87km²,raw PL/UA/BY source gap intersection essentially0. Representative points inside landmask/outside scenario_water. See seam-findings.md and .runtime/tmp/europe-precision-20260920/seam-measurements.json/poland-ukraine-seams.png. CLI read-only renderer review returned unsupported underlay/winding hypotheses; not adopted, no renderer edits. Future repair needs shared border reconciliation and missing-land corridor checks, not a wider stroke or global buffer. No seam geometry edits performed in response to the inspection request.

PL/UA seam repair now locally integrated after explicit authorization. Tool repair_tno_poland_ukraine_seams.py + geo/reviewed_seam.py: 5 reviewed gaps, 1384.5095km2 filled, 11 UA IDs, +191 coordinates; all other political geometry and all auxiliary geometry unchanged. Runtime fb1157c4ce6afe7670cd35cbcab26abbe1e33637c70686f34cedc2467db1d5a7. 12 mixed LOD combinations and strict stage/integrated contracts pass. 35 targeted tests +4 subtests pass. Browser exact runtime hash, affected RKP/RKU/HUN detail loaded, 5 original gap seeds filled/undone successfully. 16 files integrated, backup .runtime/tmp/tno-seam-20260920/baseline. Browser tab and server8005 closed/stopped. Final catalog regenerated with 663 entries and no diff; data health passed with large-file warnings only; all 18 catalog contract tests passed. CLI ed0a921592064c1284cb2561a6133301 completed test implementation; reuse from read-only to workspace_write retained stale toolset, fresh continuation fixed that. Finalization caveat: build_startup_bundle writes compact gzip but pretty raw JSON; after byte synchronization bind snapshot/audit directly through existing helpers, do not run bundle generation again or the fingerprint changes. Other inherited border gaps remain outside this repair.
