# TNO Europe precision expansion

Restore same-ID source geometry for GB, LU, IT, AT, PL, UA, BY, EE, LV, LT, ES and PT, continuing the existing FR/DE/BE/NL trial. Preserve scenario ownership, cores, historical cuts, non-target geometry and auxiliary surfaces. Preserve all pre-existing renderer/worker WIP.

Prepare source lineage and candidates under `.runtime/tmp/europe-precision-20260920/`; validate IDs, joint coverage, protected geometry, adjacency and coarse/detail consistency. Measure source coordinates, asset sizes, serial build cost and focused localhost runtime behavior. Integrate local scenario assets only after meaningful checks; no remote publication or commit requested.

CLI agents provide bounded read-only pipeline and eastern-source analysis. Parent owns all edits, candidate builds, integration and validation. Sources with incompatible identities or inadequate fidelity fail closed with explicit evidence rather than silent baseline substitution.
