# Execution context

## Current truth

2026-09-09: source conversation read through the explicitly mentioned Edge tab. Baseline 9d4b715a is newer than its fb28b187 audit. No initial WIP.
Current history_manager already narrows pure feature color UI updates. post_ready_scheduler already has epoch/execution and queued handle identity; remaining scheduler work must preserve those guards.

## Ownership

- import_transaction: interaction_funnel.js, interaction_funnel/import_apply_orchestration.js, import-only modules/tests, scenario_manager staged import preparation/application interfaces and directly related transaction modules. Shared state/actions edits require coordination.
- authority_failures: tools/verification/p4_nightly_authority.mjs and direct tests; precise P4 proof/route changes require coordination.
- remaining_scope: read-only E data/build/publish mapping.
- primary: this record set, history/scheduler remaining scope, shared interface integration, final builds/browser checks. No other agent may run shared browser/build jobs without ownership handoff.

## Live process ownership

No long process started. Short isolated unit commands are owned by the implementing agent. Before any shared test/build/server, record command, cwd, ports/output/log paths and stop criteria here.

P4 current-policy diagnostic: owner authority_failures; cwd repository root; command `node tools/check_state_writer_policy.mjs --phase P4.4 --json-out .runtime/reports/generated/nightly/p4-current-policy.json` after CLI validation; log `.runtime/tmp/editor-kernel-renewal/p4-current-policy.log`. Resource: P4 checker/cache and that output only. Run once to successful completion or classified failure; no parallel policy checker, no baseline refresh. Primary may read completed report only.

## Next step

The authorized local implementation is complete under the source plan's stated scope. Runtime source and byte-identical artifact pair are current; the same artifact user journey passes with trace off under the original110s budget, with traced failures preserved. P4 scope is closed under its narrow exit criteria; full admission remains FAIL with retained evidence. Earlier process records below are historical evidence, not active process ownership. Production promotion/rollback and full P4 admission are not claimed.

Source focused browser journey: owner primary; cwd repo; `node node_modules/@playwright/test/cli.js test tests/e2e/project_save_load_roundtrip.spec.js --grep 'editing baseline fast hoi4_1936.*roundtrip only' --workers=1 --retries=0 --output .runtime/tests/playwright/editor-kernel-source`, env MAPCREATOR_DEV_PORT=8006, PLAYWRIGHT_TEST_SERVER_PORT=8006, PYTHONDONTWRITEBYTECODE=1. Playwright owns child dev server, no reuse. Log `.runtime/tmp/editor-kernel-renewal/source-journey.log`. Stop at pass/fail or existing 110s test deadline; no duplicated run while active. Only primary manages this process and port.

Source browser session62027 ended exit1 after51s; failure at ignored177 vs1. Import agent owns fix (trusted IDs now union staged/default topology plus same-scene runtime; do not accept imported IDs as self-authorizing). Wait for final optional-layer contract and explicit bounded patch before rerunning.

Ownership additions: city_contract finished city loader/build contract + two city data files/manifest + eight references in two HOI4 strategic files. authority_failures owns all shared catalog/route and delegation contract edits. Primary owns release workflow + artifact admission verifier/tests. data_release_review performs bounded read-only review of city/release; no live process.

Scheduler source stable after waitFor resume rechecks idle state and same-key schedule cancels old executions. 46/46 target scheduler/handoff tests, 6/6 existing real history tests. Handoff uses bounded120s optional warmup lifetime, scoped scene/request/owner epoch; only explicit resource waits release the slot. Long synchronous callbacks are not magically preempted. The production contour boundary yields once; no unmeasured Worker rewrite claimed.

Next integration: finish import explicit patch and optional failure signaling, run source roundtrip plus cross-scene/failure user journey, build canonical output and a runtime artifact once source stable, run artifact shell/roundtrip and compare reproducibility. Full P4 current checker owned by authority_failures only after writer changes settle. Production action not authorized; prepare concrete reviewable result first.

Source-final two journeys: primary session77415, same8006 owned webserver, grep `editing baseline fast hoi4_1936` selects full cross-scene/raster and roundtrip-only cancellation checks; output `.runtime/tests/playwright/editor-kernel-source-final`, log `.runtime/tmp/editor-kernel-renewal/source-final-journeys.log`. Import owner final101/101 and19/19 earlier scenario tests; final explicit76-field action removes dynamic writes. Full P4 checker after-owner-fixes session66973 belongs solely to authority_failures.

Canonical Pages build: owner primary; cwd repo; `node tools/run_python.mjs tools/build_pages_dist.py`, PYTHONDONTWRITEBYTECODE=1; writes tracked dist as required by the still-default release route. Log `.runtime/tmp/editor-kernel-renewal/canonical-build.log`; one process to pass/fail, no parallel Pages builder. It may overlap source browser which serves source files, but artifact tests start only after completed build.

Source-final session77415 completed exit1: cross-scene/raster passed; roundtrip-only failed ignored628 instead of1. import_transaction resumed; import_review diagnoses independently. Canonical build35759 completed exit0; source scheduler pendingTaskKeys changed afterward so final mirror rebuild remains required.

Source lifecycle: sole owner primary; node node_modules/@playwright/test/cli.js test tests/e2e/dev/scenario_deferred_infra_lifecycle.dev.spec.js --workers=1 --retries=0 --output .runtime/tests/playwright/editor-kernel-lifecycle; Playwright owns8006 dev server. Log .runtime/tmp/editor-kernel-renewal/lifecycle.log. Existing180s test deadline, stop on pass/fail. This is instrumented source evidence, not artifact or latency improvement proof.

Source lifecycle27288 completed PASS: cold/warm TNO index12413; HOI4 switch11891; real paint/undo/redo and drained queues all passed. Scheduler queued cancellation subsequently gained explicit terminal outcomes,47/47 targeted tests pass.

Import root cause confirmed: document-refresh seeded owners from previous TNO landData before target scene runtime refresh. Fixed by moving ensureSovereigntyState after target runtime publication; caches still invalidate synchronously.12/12 transaction regressions pass. Source now stable.

Final source rerun owner primary: same two hoi4_1936 journey tests on8006, output .runtime/tests/playwright/editor-kernel-source-fixed, log .runtime/tmp/editor-kernel-renewal/source-fixed.log. Final canonical rebuild owner primary writes dist, log canonical-final-build.log. These may overlap because browser serves source. Stop each on pass/fail, no duplicated build or browser port.

Artifact-only operational path: after authorized merge, manually run existing deploy workflow at the selected source ref with artifact_only=true. It builds .runtime/pages-release/dist, checks shell and real browser flows, records a clean-source receipt, verifies downloaded bytes in the build job, then uploads Pages. Keep deploy-admission and deploy-dist from the same run together. To use the preserved tracked route, run at the approved rollback source ref with artifact_only=false; it rebuilds and requires zero tracked-dist drift. Actual promotion and deployed rollback must be exercised before deleting tracked mirrors or changing the default. No local dirty-worktree comparison counts as a release receipt.

Structural import follow-up: policy schema rejected new omnibus76-field writer because fields already have domain owners. import_transaction now coordinates existing state/actions; authority_failures owns contract and legitimate policy migration. Earlier source/build runs remain scoped behavior evidence; final source mirror will be rebuilt after this owner-only refactor.

Import identity runtime diagnostic: soleowner primary, local8006 Playwright server; config .runtime/tmp/editor-kernel-renewal/import_diagnostics.config.cjs. Captures actual current/staged/base topology, land and runtime ID intersections to explain176 legitimate coarse IDs; no project mutations.90s deadline; log import-identity-diagnostics.log. Previous source-fixed86923 ended1 (full passed; roundtrip177), canonical63574 ended0 before owner refactor.

Diagnostic config stopped at spec syntax parsing before page load; no runtime evidence from that attempt. Reviewer independently established exact source from source-fixed save:209 primary political IDs,32 overlapping HOI4 baseline;177 extra saved entries exactly match trusted data/europe_topology.json primary. Staging must union topologyPrimary || topology political IDs, never arbitrary old landData/detail. No diagnostic rerun needed.

Source owner refactor and primary-topology ID correction now frozen. Existing13 transaction tests include trusted primary/fallback IDs and reject foreign landData/detail/FORGED. Final source targeted rerun soleowner primary: roundtrip-only test,8006 owned child server, output .runtime/tests/playwright/editor-kernel-source-accepted, log source-accepted.log. Stop on pass/fail, then final build pair.

Final source+owner build pair: soleowner primary; canonical node tools/run_python.mjs tools/build_pages_dist.py -> dist (canonical-owner-build.log), then independent same builder --output-root .runtime/pages-release/dist (artifact-build.log). Sequential builders, source frozen. Diagnostic compare uses existing tree snapshot/manifest integrity functions and explicitly admission=false for uncommitted source. Artifact shell and served browser checks begin after both complete.

Final preflight55/55 transaction/file/sample tests pass after no-scenario detail and summary corrections. Scheduler independent review PASS with47/47. Canonical accepted builder session62760 (log canonical-accepted-build.log) now includes these final fixes; earlier canonical-owner build precedes no-scenario correction.

Final build pair62760/34506 completed0,922.06MiB each. Source import independent review PASS WITH NOTES,14/14 independently rerun. Primary now owns artifact verification: comparison script compare_artifacts.py; artifact shell63 tests with SCENARIO_FORGE_PAGES_ARTIFACT_ROOT=.runtime/pages-release/dist, log artifact-shell.log. Primary artifact HTTP server binds127.0.0.1:8006 via Python312/python.exe; PID saved artifact-server.pid, logs artifact-server.log/.err.log. No browser test may use8006 concurrently. Stop server by this exact PID after public gate and3artifact journeys finish. All browser URLs explicitly loopback; no deployment.

Artifact comparison59075 PASS:9565files,966850155bytes, both trees175c8bc784b6e42d718a907347d1fcc995c0ec5aa4d5e2f554e169cc63bdf0c1. Diagnostic admission=false. Artifact public gate40560 PASS30.3s, console/network issue count0. Artifact journeys49250 running3tests: natural background first test passed, with real6queued tasks at input; native pointerdown queue0.5ms, input-to-visible66.3ms, input-to-stable7421.6ms (includes250ms hold). Single observation, distinct boundaries, no p95 or paired speedup; do not add overlapping stage durations. Edit survived natural drain and immediate undo passed. Raw evidence natural-background-input-evidence.json under artifact-journeys output.

Artifact shell11919:62/63 passed; sole failure was old structural assertion hardcoding path:dist despite intended default-preserving expression. Updated exact path expression while retaining hidden-files requirement. Failed test plus7admission tests all8passed in artifact-shell-targeted.log. Other62 unaffected checks not repeated. App artifacts unchanged by test-only fix.

All artifact3journeys49250 completed0 in3.5m. Artifact Python server30616 identity verified and stopped. No primary browser/build/server process remains. P4 full checker75106 completed with real784violations report; authority_failures solely owns trusted official generator wrapper62937. import_review and data_release_review now provide bounded read-only unsupported/ledger triage. Root investigates historicalproof path semantics (official generator supports proven path expansion without refreshP4Baseline), no app source changes planned.

P4 reader proof requested a behavior-preserving source shape change: two getScenarioImportValidFeatureIds forEach loops now use for...of, leaving Map callback unchanged.14 transaction tests passed (import-reader-loop-tests.log). Prior artifact hashes/journeys refer to source before this proof-oriented loop change; final canonical/artifact rebuild and focused roundtrip will follow once policy owner confirms no more production-source changes. Do not present prior tree identity as final current bytes.

P4 review caught a real stale-import bug: captureImportDocumentIdentity read a nonexistent top-level scenarioApplyEpoch. It now reads target.renderTransactionDiagnostics?.scenarioApplyEpoch ?? 0, matching the actual renderer epoch owner and treating lazy initialization as epoch zero. The FileReader-pending regression advances the real nextScenarioApplyEpoch while dirtyRevision/request ID remain unchanged; before the fix it incorrectly committed, afterward it cancels. Transaction15/15 and current transaction/file/sample56/56 pass (import-epoch-final-tests.log, import-final-combined.log). No new top-level state key was authorized.

The chunk payload dictionary clone now uses Object.fromEntries(Object.entries(...)) instead of a borrowed object spread, preserving string-key dictionary behavior and O(n+k) cost while satisfying the existing strict action verifier. Chunk state action22/22 pass (chunk-action-final-tests.log). Authority owns refreshed exact source proofs and native scan diagnostics. Both latest production corrections still require final artifact rebuild/focused journey.

Current-source build pair: sole owner primary, canonical builder session15103 uses tools/build_pages_dist.py and log canonical-p4-final-build.log, followed sequentially by --output-root .runtime/pages-release/dist. No parallel Pages builder. This may overlap authority-only historical proof analysis. Further production-source fixes, if required, invalidate this pair and will trigger only the affected rebuild/verification. Artifact browser work starts after both builders finish.

Canonical15103 passed. Artifact builder refused to overwrite the previous nonempty dist output; automatic approval blocked the attempted native PowerShell cleanup, so the old directory was retained. Current artifact builder58518 instead uses a new .runtime/pages-release/p4-final-dist directory, log artifact-p4-final-build.log. The reproducibility comparison now targets that current directory.

Artifact58518 passed. Primary owns comparison64673 and focused artifact browser92220 (roundtrip-only hoi4_1936, workers1/retries0, existing110s deadline); output .runtime/tests/playwright/editor-kernel-artifact-p4-final, log artifact-p4-final-journey.log. Native Python HTTP server30040 serves only .runtime/pages-release/p4-final-dist at127.0.0.1:8006; both explicit browser base URLs use localhost. Stop this exact owned PID after the focused check. Authority remains sole owner of P4 historical/current scans.

Comparison64673 passed: both current trees6bad90b11b2f3b55b568797dda9e8a5b2ed2c8797e5ae9f079a0b5616dd98d75,9565files/966850264bytes. Browser92220 failed the unchanged110s total deadline after actual import/save and Modern World transition; no behavior assertion failure recorded. Earlier corresponding artifact journey took1.4m. Historical P4 CPU proof has just ended; root owns one isolated rerun65505 under the same budget/workers/retries and same artifact/server, output editor-kernel-artifact-p4-isolated and log artifact-p4-isolated-journey.log. Authority was asked to defer the next heavy fresh scan until this bounded browser run ends. Contention is a hypothesis, not a confirmed cause; retain the failed run.

Isolated65505 also exceeded110s, at the Modern World transition after successful import/save. P4 competition is therefore not established as the cause. Both failures retained; import_review is comparing trace spans against the earlier success. Owned artifact HTTP server30040 was identity-verified and stopped. No primary browser/build/server process remains.

Original source conversation was re-read to enforce its phase A stop condition: source-level explanation, targeted checks and failure reports; explicitly no extended recovery project or broad baseline refresh. Authority's bounded scope PASS:93/93 tests,3/3 proof tests, schema/action/reader/cross-file/replacement contracts[], final routing changed187/owned63/unmatched0/gaps0. Canonical policy contains only15 affected import owner updates; baseline/baselines/progress and all other writers equal trusted HEAD. Full P4 FAIL remains documented in784 original report and96 candidate progression entries. Candidate never replaced canonical policy. Cache/legend live write debt and read-only over-approximation have source-level triage. No full-gate acceptance claimed and no further broad owner migration planned.

Trace reviewer: failed runs have10–13s click/undo/redo API spans; snapshot collection itself totals only50–173ms per three-snapshot group, so snapshot capture alone does not explain the delay. Earlier PASS has no retained trace; logs show27.65/32.23s extra time before Modern transition in the failures. No supported attribution to the latest three source corrections. Primary owns one controlled experiment7446: identical artifact, assertions,110s total budget/workers1/retries0, only CLI --trace off (existing project latency tests document recording perturbation). Server37708 serves p4-final-dist on127.0.0.1:8006; logs artifact-p4-no-trace-server.*, output editor-kernel-artifact-p4-no-trace and artifact-p4-no-trace-journey.log. Stop exact owned server after completion. No permanent test configuration change.

Controlled experiment7446 PASS: all current-artifact roundtrip-only functional assertions completed in about85s (Playwright1.4m test/1.5m total), under unchanged110s budget. Sole experimental change was --trace off; no production/test source, assertions, waits or timeout changed. Recording is implicated as a contributor, not a quantified isolated root cause or app performance gain. Two failed traced runs remain preserved. Owned server37708 identity verified and stopped; all implementation agents and read-only reviews are complete. Current local implementation complete; full P4 and actual deployment remain unclaimed.

User authorized remote merge/push and cleanup of accumulated worktrees/branches, preserving recent gains. Fresh fetch shows origin/main fb28b187 and existing PR127 head9d4b715a (local main tip); main has enforced6required checks. PR127 has all specialized/perf checks green but pr-fast fails on12unmatched adaptive-selector files. Root moved current WIP to the existing PR head branch codex/editing-runtime-optimizations-20260909 without modifying main or dropping files. Root sole Git/CI/build/server owner; import_review audits8historical worktrees, data_release_review audits7, authority_failures owns only actual selector metadata/routing repair and targeted tests. No worktree cleanup before ancestry/coverage/WIP checks and recovery evidence. Existing tracked source/dist pair remains validated; metadata-only fixes will not trigger unnecessary browser repetitions.

Inventory complete:15historical worktrees have no uncovered optimization. Most are ancestors or patch-equivalent;5d0c P1 commits were aggregated into bd20ccc5/1b2638, b52a's remaining wrapper was deliberately retired, and d081's proof implementation was superseded by stricter current contracts. The57ef and m3-shadow trees contain only3old generated-dist modifications each; binary patches and original files were saved. Critical M0-M3/P1 reports and raw browser evidence were copied to .runtime/tmp/editor-kernel-renewal/worktree-recovery:359files/179225200bytes, no missing patterns. Windows long-path handling was required for attachments. Before-integration branch/worktree refs and current tracked patch are retained alongside this archive.

Actual PR selector repair is limited to3tool/test files;189cumulative changed files now have0unmatched,15explicit nonbehavioral entries, and76/76target checks. No runtime bytes changed, so reuse the completed current artifact checks. Root will commit the stable source+artifact+data+release integration, archive original Git tips, then remove only covered worktrees/branches and push the updated existing PR through enforced checks.
