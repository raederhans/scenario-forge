# Execution context

PR #175 first CI: all scenario contracts, browser smoke and performance passed, but Pages hero reproduction and the timeout structural gate both failed. The affected-contract lane caught the new E2E's 120-second total timeout exceeding the existing 60-second guardrail. Corrected the test to 60 seconds without modifying the allowlist, guardrail or product data. The exact failing structural test now passes; root reran native hit/fill/undo with the 60-second cap and it passed in 44.9 seconds. Owned rerun server 44901 stopped; browser process 48184 exited 0. Follow-up commit a224217e fixed the timeout but the stale hero still blocks Pages acceptance; see the correction below.

Latest integration handoff: current-main rebuild, targeted validators and browser acceptance all completed successfully. Root-owned server 61395 and test 12288 stopped/finished. Final candidate adopted to integration checkout after old-source concurrency guards; 29 scenario paths changed, political chunks/mesh pack/current-main locales unchanged. Source HGO was read-only through its explicit environment path. All 82 unit/regression tests pass, and new verification classification/route checks pass. Final code review found no material blocker. Root is authorized to commit, push, create PR, monitor required checks and merge; preserve the dirty primary checkout. No local build/test process remains active.

2026-09-26: user authorized preceding plan steps 1–3. Working directory `C:/Users/raede/Desktop/dev/mapcreator`. Extensive unrelated UI/rendering and locale WIP exists; do not stage/revert it. No geometry-tool/scenario-geometry WIP was present at start. Source HGO lives at `historic geographic overhaul` and its bitmap is available. Current main history includes September 11 geometry and September 25 ownership updates.

Root is the sole builder/adoption/browser owner. Delegates may independently inspect source data and write only assigned tools, tests, or isolated probe outputs. No delegate runs full scenario builders or canonical writes. Runtime root for this task: `.runtime/tmp/atlantropa-expansion-20260926`; reports: `.runtime/reports/generated/atlantropa-expansion-20260926`.

Current: source inventory finished (4,655 regional rows / 3,002 unique provinces). Treat unresolved output as a diagnostic, not automatic missing geography. Trial admissions: Marmara 9038/18349 TUR, followed only after acceptance by Constantine 9070/18332 IAL. Detailed deferrals are in source-decisions.md.

Baseline frozen at `.runtime/tmp/atlantropa-expansion-20260926/baseline/tno_1962` (442 entries), with selected source snapshots and initial status. Root starts a baseline rebuild to establish whether current generator reproduces the published data before any new candidates: `py -3 -B -m tools.rebuild_atlantropa_stage --source-dir .runtime/tmp/atlantropa-expansion-20260926/baseline/tno_1962 --stage-dir .runtime/tmp/atlantropa-expansion-20260926/baseline-rebuild/tno_1962`, cwd repository. Exclusive output: baseline-rebuild; log: `.runtime/tmp/atlantropa-expansion-20260926/baseline-rebuild.log`. Success: exit 0 with strict contracts; failure: preserve diagnostics and inspect, no blind retry or canonical writes. Non-owners may read completed log snapshots only.

Baseline normalized geometry reproduces all land and owners (CRO9 numerical removed area 1.78e-17 deg2); full materialization still running. Root starts the sole full source-inventory process: `py -3 -B tools/audit_atlantropa_sources.py --baseline-dir .runtime/tmp/atlantropa-expansion-20260926/baseline/tno_1962 --output-dir .runtime/tmp/atlantropa-expansion-20260926/inventory`, cwd repository, log `.runtime/tmp/atlantropa-expansion-20260926/inventory.log`. This reads source and writes only inventory output, independent of builder destinations. Success is eight bounded AOI inventories plus explicit non-georeferenced Black Sea source index. Failure is retained and diagnosed. Delegate unit tests passed, actual dataset execution pending.

Both baseline build and inventory have exited 0. The build's safe repair is materialization, not a strict validation verdict. Baseline materialization exposed one unchanged shoal owner/core drift (ATLSHL_west_med_6 ATL -> IBR); fixed with an exact geometry plus donor-identity guard, including map preservation (12 scoped rebuild tests pass). Unchanged political chunks reordered features; runtime preserve_chunks.py restores only semantically identical political files and corresponding old manifest descriptors before refreshing derived contracts.

Root launched Turkey-only stage: `py -3 -B -m tools.rebuild_atlantropa_stage --source-dir .runtime/tmp/atlantropa-expansion-20260926/baseline/tno_1962 --stage-dir .runtime/tmp/atlantropa-expansion-20260926/turkey-v1/tno_1962`. Owner root; exec session 95274; output turkey-v1 only, log turkey-v1.log. Exit 0 is required before explicit geometry, named-water, coastline and strict-contract acceptance. On failure retain diagnostics; no canonical adoption. Frozen app at frozen-app will later serve only the candidate scenario on localhost:8007 for root-owned browser tests.

Turkey v1 exited 1 before scenario materialization: named-water compiler dropped genuine components below 1e-10 deg2, failing the existing 1e-12 difference gate. Scoped clipping now restores fully dropped source components with D3 winding; no threshold relaxed. The pre-classification helper identity guard now derives layer correctly and preserves uniquely matching unoccupied old IDs (ATLWLD_aegean_89 -> 88). Real v1 normalized probe and 13 scoped rebuild tests pass. Root launched Turkey v2 using the same source and `turkey-v2/tno_1962` destination, session 46709, log turkey-v2.log; same acceptance/failure contract. No canonical data adopted yet.

Turkey v2 and the post-materialization semantic chunk preservation exited 0. Custom expansion, source/chunk/baseline geometry, full water validation, coastline acceptance and strict scenario contracts all PASS. Old land removal is only 1.76e-17 deg2; new land 0.0870484241 deg2; old assignments preserved; only Marmara and Bosporus/Dardanelles named-water geometries cut. During scope review, regenerated political coarse chunk changed 3,063 existing geometries through fresh simplification despite identical runtime political inputs. The scoped builder is being tightened to retain all original political chunks only after unchanged political source and assignment proof, with matching manifest chunk ID/URL sets. Turkey stage will receive this preservation and contract refresh before use as the Africa source. No canonical adoption yet.

All 192 original political chunks and descriptors were restored after full political source/assignment equality proof; strict Turkey contracts re-passed. Formal scoped builder now includes that preservation, with 14 unit tests passing. The final inventory tool rerun exited 0 with the same 4,655 rows / 3,002 unique provinces (`inventory-final`). Root added Constantine 9070 -> IAL and launched accumulated final stage from validated Turkey v2: `py -3 -B -m tools.rebuild_atlantropa_stage --source-dir .runtime/tmp/atlantropa-expansion-20260926/turkey-v2/tno_1962 --stage-dir .runtime/tmp/atlantropa-expansion-20260926/final-v1/tno_1962`; root owns session 17132 and final-v1.log. Acceptance requires original frozen-baseline checks plus final localhost hit/fill/undo before guarded canonical adoption. No remote publication.

Final v1 build exited 0; geometry, water, coastline and strict contracts PASS. Custom assignment preservation FAIL correctly blocked adoption: west_med shoal enumeration forms an occupied-ID chain (old2 -> generated4, old3 -> generated5, old5 -> generated7, plus genuinely new shoal uses generated2). Full geometry/lineage matching must preserve all old identities simultaneously and allocate a fresh ID for the new shoal. Delegate owns only rebuild tool/tests for that fix; root will rebuild final-v2 from validated Turkey-v2. Both new land polygons have measured boundary contact (0.910115 and 0.480538 degrees), with WGS84 areas about 816.26 and 1,718.35 km2. No old render-land area loss in the serialized result.

To catch interaction failures while the identity fix is prepared, root owns immutable final-v1 preview on localhost:8007 (serve_candidate.py session 51895, frozen app) and focused two-case Playwright run session 51899. Output `.runtime/tests/playwright/atlantropa-expansion-20260926-v1`, browser-v1.log and dedicated timing/failure JSON. No other task may use this server or test output. Success is native target hit/fill/undo and existing coastline toggles; on failure preserve trace and diagnose. Stop the owned server via its PTY after the run. Final artifact still needs final-v2 acceptance; browser-v1 is preliminary evidence.

Final state: complete. Final-v2 builder session 32582 exited 0. All explicit geometry/water/coastline/strict-contract/old-assignment gates passed against the original frozen baseline. Helper reconciliation now handles occupied-ID chains, preserving 78 existing helpers; 192 political chunk files remain byte-identical. The root-owned final preview server (session 18697) served immutable final-v2 with the frozen application on localhost:8007. Coastline visibility test passed (16.5s). Native two-land hit/fill/undo test passed (36.5s) after correcting test camera scale and using widest interior points with SVG screen-coordinate conversion; the failed exploratory runs remain diagnostic evidence, not acceptance. Final browser log is browser-edit.log; preceding browser-final.log has the successful coastline test plus the earlier failed hit probe.

Guarded adoption copied 30 changed files to canonical TNO after all destination concurrency guards passed. Every candidate file then matched canonical bytes. Catalog regenerated without content changes; data health PASS (10 report-only large-file warnings), 19 catalog contract tests PASS, and 19 inventory/rebuild unit tests PASS. No commit/push/deployment. All root-owned build, validator, test and catalog sessions exited; root-owned preview servers were stopped with Ctrl-C. Pre-existing user server was untouched. Final decisions, limitations and evidence are in results.md; full Black Sea remains later scope.

Integration 2026-09-26: user authorized check, merge and push. Root is integration owner. Branch codex/atlantropa-expansion-20260926 starts at origin/main a5eed904 in C:/Users/raede/.codex/worktrees/atlantropa-merge/mapcreator. Source main checkout stays untouched with unrelated WIP. Rebuild from this checkout's current TNO source preserves PR174 political geometry and latest startup locales; no old generated data copied wholesale.
Live process: root owns `py -3 -B -m tools.rebuild_atlantropa_stage --source-dir data/scenarios/tno_1962 --stage-dir .runtime/tmp/atlantropa-integration/candidate/tno_1962`, cwd this integration worktree, donor env SCENARIO_FORGE_HGO_ROOT points to primary checkout's read-only HGO source. Log `.runtime/tmp/atlantropa-integration/rebuild.log`; only candidate/diagnostics outputs. Exit 0 permits targeted geometry, water, strict contract and current-app browser acceptance. On failure retain diagnostics, no publication. Non-owner reviewer may read completed source/log snapshots only.

Integration review: bounded independent reviewer found no material blocker in the scoped tools/tests. Local 19 inventory/rebuild tests and 63 existing Atlantropa regression tests pass. Fixed real verification-discovery failures by classifying both geospatial test modules and registering the new source-inventory route; heavy classification and 652-route schema pass. E2E manifest/import graph checks pass. Root remains sole builder/browser owner.
Next processes after successful rebuild: read-only `check_atlantropa_geometry.py`, `validate_tno_water_geometries.py`, `check_scenario_contracts.py --strict`, and runtime `check_expansion.py` against the frozen current-main baseline, independent report files under `.runtime/reports/generated/atlantropa-integration/`. Only all-pass permits guarded copy into this checkout's scenario. Then root starts `py -3 -B tools/dev_server.py --port 8007 /app/` with MAPCREATOR_OPEN_BROWSER=0, and runs `npx playwright test tests/e2e/tno_atlantropa_coastline.spec.js --workers=1 --output=.runtime/tests/playwright/atlantropa-integration` with explicit localhost:8007 base URL. Browser log `.runtime/tmp/atlantropa-integration/browser.log`; exit 0 and both tests passing required. Stop owned server via PTY Ctrl-C; retain all diagnostics on failure. CI owns hosted Pages, smoke and required PR gates after push.

Correction after full failure-log review: both the first and second PR verification runs failed the TNO landing hero byte-reproduction check; the first also failed the timeout guardrail. Advancing to later workflow steps did not prove Pages success because the job continues to affected contracts. Previous Pages-pass narrative is superseded. The remaining actual failure is stale hero-tno-1962.svg after the admitted coastline additions. Root will regenerate hero scenario maps into `.runtime/tmp/atlantropa-integration/hero`, copy only TNO SVG/metadata after checking unrelated outputs unchanged, rasterize only TNO WebP using the existing tool, and run the exact builder-output contract. Owner root; cwd integration worktree; logs hero-build.log, hero-raster.log, hero-test.log. No new scenario geometry build or assertion/allowlist relaxation.

Hero synchronization completed: only the TNO SVG, metadata and WebP changed; all three other generated scenario heroes remained byte-identical. Existing rasterizer completed successfully. Exact PagesDistStartupShellTest.test_landing_hero_scenario_assets_match_builder_output passed (80.930 seconds). No local build/test process remains active. Fresh remote CI is pending the follow-up push.
