# Atlantropa 前三步实施结果

## 合并前复核（2026-09-26）

用户追加授权检查、合并与推送。交付分支 `codex/atlantropa-expansion-20260926` 从最新主线 `a5eed904` 隔离建立，重新生成全部本轮场景产物，保留 PR #174 的法国／阿尔及利亚政治区修复。最终交付涉及 29 个场景文件；当前主线的启动语言文件无改动，192 个政治区分块及政治边界网格逐字节保留。

本次整合重新通过来源／重建 19 项测试、既有 Atlantropa 回归 63 项测试、完整水域验证、源／粗细分块几何与非目标基线保留、旧归属和新增海陆互斥检查、严格场景契约（0 errors / 0 warnings）。当前主线应用上的两项浏览器测试通过（海岸线 24.7 秒、两块新地实际点击／填色／撤销 44.9 秒）。Catalog 重新生成无内容变化，数据健康检查通过，仅现有大文件体积提示。

合并检查还补齐了两份地理测试的依赖分类与新清单测试的执行路由。独立代码审查未发现实质阻断问题；网格保留增加了 JSON 内容全等前提和回归断言。整合证据位于隔离工作树 `.runtime/reports/generated/atlantropa-integration/`。远端 CI 与合并状态以 PR/Git 回执为准；主工作区其他未归属工作未覆盖。

## 首次本地实施记录

2026-09-26：完成来源盘点、土耳其／马尔马拉海候选接纳、北非／西地中海候选接纳。通过验收的最终 `final-v2` 已写回本地正式 TNO 场景，30 个场景文件发生变化。未提交、推送或部署。全黑海盆地不在本轮实施范围。

## 本轮成果

1. 新增可复用盘点入口 `tools/audit_atlantropa_sources.py`。扫描八个既有区域及 0.35 度邻接带，得到 4,655 条区域／省份记录，涉及 3,002 个不同来源省份。`unresolved` 是人工复核队列，不是已确认漏地数。全黑海只有明确标注的非空间来源索引。
2. 接纳 Marmara 来源州 `9038`、省份 `18349`，新增 `ATLPRV_18349`，归属与 core 为 `TUR`。最终新增陆地约 **816.26 km²**，与原海岸有实际边界接触。
3. 接纳 Constantine 来源州 `9070`、省份 `18332`，新增 `ATLPRV_18332`，归属与 core 为 `IAL`。最终新增陆地约 **1,718.35 km²**，与原 `DZA-2166` 海岸相接。

| 层 | 本轮前 | 本轮后 |
| --- | ---: | ---: |
| 陆地 | 163 | 165 |
| 浅滩 | 69 | 70 |
| ATL 水域片段 | 493 | 495 |
| 合计 | 725 | 730 |

上述面积使用最终经纬度多边形的 WGS84 测地面积，不是地理真实精度承诺。水域片段数量增加来自重新分割，不代表水域面积增加。

## 为确保精度与旧数据保留所作的修正

- 命名水域仅裁去新增陆地。实际影响 `tno_sea_of_marmara`、`tno_bosporus_dardanelles`；源 GeoJSON 与运行时 TopoJSON 同步，原属性保留。恢复编译器会丢弃的真实微小组件，没有放宽 `1e-12 deg²` 几何差异门槛。
- 根据唯一几何与来源对应关系保留辅助地块身份，避免新浅滩插入后编号链式移位覆盖既有 owner/core。78 个旧辅助地块身份保留，旧有归属通过增量检查。
- 完整政治区来源与归属不变时保留原政治区分块，避免重建带来非目标简化。192 个政治区分块逐字节与冻结基线一致。
- 正式写回前逐一检查目标文件仍与本轮起始基线一致。写回后候选与正式场景所有文件逐字节一致。既有 locale 人工内容通过基线保留检查；其他 UI、渲染器与语言工作未被本轮覆盖。

## 实际验收

- 来源清单与隔离重建目标单元测试：19 项通过。
- 最终来源／粗细分块几何、非 ATL 数据保留：通过，0 个问题。
- 完整 TNO 水域几何验证：通过；命名水域与新增陆地重叠约 `3.06e-17 deg²`，低于既有容差；旧陆地移除面积为 0。
- 严格场景契约：通过，0 errors、0 warnings；海岸线判定为 `scenario_accepted`。
- localhost 浏览器：既有海岸线可见性切换测试通过；两个新地块实际点击命中、填色、撤销测试通过，未捕获页面 JavaScript 异常。浏览器读取本轮起始冻结应用与最终候选数据，避免其他 UI 工作中的变动污染本轮结果。
- 浏览器首次定位脚本的过大滚轮缩放及低缩放边界点击已修正；最后使用 SVG 交互层坐标与内部最宽处点击，原生鼠标操作，没有直接改写填色或历史状态。最终两项测试分别用时 16.5 秒、36.5 秒。预览环境另有 `/assets/sample-runs.json` 404，本轮没有声称全站网络无错误。

数据 catalog 已重新生成（666 条，内容无差异）；data health 通过，0 errors、10 条现有大文件提示；另有 19 项 catalog 契约测试通过。数据健康提示仅报告体积，不是几何或契约失败。

## 保留的后续工作

- **Oran／直布罗陀东侧（9069 / 18320）**：源候选同时触及 IBR 与 ALC，需先找到可信边界拆分；不以单一归属接纳整个区域。
- **Benghasi（9084 / 18310）**：当前配准只有点接岸，不能据此生成陆桥。
- **Lesvos／Chios（9843 / 19191、9844 / 19190）**：通用仿射落点错误，需独立岛屿锚点。
- **未命名模板 14164、14176、13049、14284**：身份、归属或区域配准仍不明确。
- **法国、亚得里亚海及其余既有范围**：完成盘点，未据未覆盖面积自动添加地块；保留现有几何，待逐项解决清单中的来源或配准疑点。已有覆盖的 Gemlik、Dam、Cyprus 与重复配准残余不重复加入。
- **黑海全盆**：留待下一阶段专门配准与验收。

原始位图为 5120 × 2560，横向一像素约 0.0703125 度；0.0025 度是生成流程的简化参数，不是绝对配准误差上限。保留来源支持的轮廓，不为视觉平滑抹除细节。

## 证据位置

- 来源清单：`.runtime/tmp/atlantropa-expansion-20260926/inventory-final/source_inventory.{json,txt}`。
- 最终报告：`.runtime/reports/generated/atlantropa-expansion-20260926/final-{geometry,water,contracts,expansion,coastline,preservation}.json` 与 `adoption.json`。
- 对照图：`.runtime/reports/generated/atlantropa-expansion-20260926/additions.png`，橙色为新增陆地。已确认图中新增几何与最终采纳结果一致。
- 浏览器最终海岸线结果：`.runtime/tmp/atlantropa-expansion-20260926/browser-final.log` 中第一项；最终编辑结果：`browser-edit.log`。截图位于 `.runtime/tests/playwright/atlantropa-expansion-20260926-edit/`。
- 详细来源与延期依据：[source-decisions.md](source-decisions.md)。
