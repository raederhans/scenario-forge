# TRANSPORT GLOBAL DATA CHAIN CLOSURE 2026-04-15

## Goal
- 修复当前 global transport 数据链里已经确认的坏引用和 stale 产物问题。
- 先收口 global road 数据链，再补齐 global rail 正式 checked-in 产物。
- 保持主地图 runtime / UI 开关继续关闭，不在这一波提前接通 `showRoad` / `showRail`。

## Plan
- [x] 复核并修掉当前 global transport 的坏引用与 eager loader 问题
- [x] 收口 global road catalog / shard 真相 / stale 目录与过时 manifest metadata
- [x] 补齐 global rail manifest / audit / topo / placeholder sidecar / catalog
- [x] 运行静态与单元验证
- [x] 做最终 review，更新 lessons learned，并把本文档移入 archive

## Progress
- 2026-04-15 复核完成：当前确认问题包括：
  - `js/core/data_loader.js` 仍硬编码指向不存在的 `global_road` / `global_rail` 顶层 topo/geojson 文件；
  - `global_road/shards` 实际目录数 43，但当前 builder 真正定义的 shard 只有 39；
  - 当前正式 shard 中有 6 个 manifest 的 `build_command` 仍是旧写法；
  - `global_rail` 目录里还只有 `source_recipe.manual.json`，没有正式 checked-in 产物。
- 2026-04-15 已完成：
  - 已把 `global_road` / `global_rail` 从默认 eager context layer 入口中移除，避免启动阶段再请求不存在的顶层 pack；
  - 已删除 4 个 stale road shard 目录：`e000_e002`、`e002_e004`、`e010_e015`、`e100_e110`；
  - 已统一重写当前正式 road shard manifest 的 `build_command` 元数据；
  - 已新增 `tools/build_global_transport_catalogs.py`，并生成 `data/transport_layers/global_road/catalog.json`。
- 2026-04-15 进行中：
  - 已对 `global_rail` 顶层全量 build 做过一次真实试跑：进程在只写出 recipe 后持续高 CPU / 高内存运行，但在观察窗口内没有任何正式输出文件。
  - 基于这次结果，本波先完成“问题修复 + road 数据链收口”；rail 保留为下一波，优先补 phase 日志和是否需要 shard 的真实诊断，再决定是继续单 pack 还是切到 shard / catalog 路径。
- 2026-04-15 追加完成：
  - 已给 `tools/build_global_transport_rail.py` 补上 phase log：开始扫描、扫描 checkpoint、preview assembly、full assembly、phase-B placeholder sidecar 写出。
  - 已给 rail 增加 region-priority 简化策略：`japan / europe / russia / east_asia / north_america` 保持主关注区基线，其它地区改为更高长度阈值、更粗 simplify、并且提前丢掉 unnamed / unknown 的低优先级线段。
  - 已把 rail 扫描 batch 降到 `50_000`，并把 normalized parquet spill 改成按 `TARGET_NORMALIZED_CHUNK_ROWS=4000` 聚合后再 flush，避免继续生成大量极小 chunk 文件。
  - 已完成一次新的 rail 受控后台评估：前 250 个 scan checkpoint 内，日志显示仍然只扫到了 `low_priority` 区域，说明当前数据源的扫描顺序本身就不按我们的关注区排列；新的 region-priority 策略已经显著压低了保留量，但还不能解决“先扫描到非重点地区”这个根问题。
  - 当前观察到的新 rail 评估指标：约 50 秒时 `raw_seen=21870`、`kept=399`、`pending_rows=399`，私有内存约 `4.9 GB`。相比之前 7GB+ 且没有可解释日志，已经更可观测，也更容易继续做下一步诊断。
- 2026-04-15 region/shard 改造启动：
  - rail builder 已从“单个 global pack”切到“focus region + shard”合同，当前 shard 路径为 `data/transport_layers/global_rail/regions/<region>/shards/<shard>/...`。
  - 已新增 focus region / shard 真相、manifest region+shard metadata、以及 rail catalog builder 的 region-sharded 形状；当前 rail catalog 仍不会默认生成，直到 focus shards 正式产物补齐。
  - `japan / jp_e128_e147` 已完成真实构建并落正式文件：`manifest.json`、`build_audit.json`、`railways.preview.topo.json`、`railways.topo.json`、`rail_stations_major*.geojson`、`source_recipe.manual.json`。
  - `europe / eu_w012_e010` 已开始真实构建并确认 scan checkpoint 稳定推进，但在本轮观察窗口内尚未写出正式产物；对应仅写出 `source_recipe.manual.json` 的半成品目录已删除，避免留下 stale rail shard。
- 2026-04-15 Europe 完成：
  - `europe / eu_w012_e010` 已完成真实构建，正式文件已落盘。
  - `europe / eu_e010_e025` 已完成真实构建，正式文件已落盘。
  - `europe / eu_e025_e045` 已完成真实构建，正式文件已落盘。
  - 当前 Europe 三个 shard 的正式计数如下：
    - `eu_w012_e010`: preview `1553` / full `8958`
    - `eu_e010_e025`: preview `1509` / full `9540`
    - `eu_e025_e045`: preview `1624` / full `5976`
  - Europe 部分现在已经可以视为“region/shard 路线跑通并且正式产物存在”的状态；下一步可继续扩到 `east_asia / russia / north_america`，最后再统一生成 rail 顶层 catalog。
- 2026-04-15 East Asia 完成：
  - `east_asia / ea_e095_e115` 已完成真实构建，正式文件已落盘。
  - `east_asia / ea_e115_e130` 已完成真实构建，正式文件已落盘。
  - `east_asia / ea_e130_e150` 已完成真实构建，正式文件已落盘。
  - 当前 East Asia 三个 shard 的正式计数如下：
    - `ea_e095_e115`: preview `1065` / full `3865`
    - `ea_e115_e130`: preview `959` / full `4611`
    - `ea_e130_e150`: preview `276` / full `2147`
  - East Asia 三个 shard 是通过一条长监控串行跑完的，builder 日志已经覆盖从 scan checkpoint 到 preview/full assembly 的完整阶段。
- 2026-04-15 Russia 完成：
  - `russia / ru_e030_e060` 已完成真实构建，正式文件已落盘。
  - `russia / ru_e060_e090` 已完成真实构建，正式文件已落盘。
  - `russia / ru_e090_e120` 已完成真实构建，正式文件已落盘。
  - `russia / ru_e120_e150` 已完成真实构建，正式文件已落盘。
  - `russia / ru_e150_e180` 已完成真实构建，正式文件已落盘。
  - 当前 Russia 五个 shard 的正式计数如下：
    - `ru_e030_e060`: preview `822` / full `4488`
    - `ru_e060_e090`: preview `367` / full `1280`
    - `ru_e090_e120`: preview `137` / full `732`
    - `ru_e120_e150`: preview `149` / full `949`
    - `ru_e150_e180`: preview `0` / full `4`
  - Russia 五个 shard 也是通过一条长监控串行跑完的；`ru_e150_e180` 只留下极少量 backbone，说明当前降精度策略在远东尾段已经生效。
- 2026-04-15 North America 完成：
  - `north_america / na_w170_w140` 已完成真实构建，正式文件已落盘。
  - `north_america / na_w140_w110` 已完成真实构建，正式文件已落盘。
  - `north_america / na_w110_w080` 已完成真实构建，正式文件已落盘。
  - `north_america / na_w080_w050` 已完成真实构建，正式文件已落盘。
  - 当前 North America 四个 shard 的正式计数如下：
    - `na_w170_w140`: preview `10` / full `64`
    - `na_w140_w110`: preview `291` / full `1999`
    - `na_w110_w080`: preview `1356` / full `8159`
    - `na_w080_w050`: preview `220` / full `2386`
  - North America 四个 shard 也是通过一条长监控串行跑完的；之前的 `na_w110_w080` 半成品 recipe-only 目录已清掉并重跑成功。
- 2026-04-15 rail catalog 完成：
  - 已生成 `data/transport_layers/global_rail/catalog.json`。
  - 当前 rail 顶层 catalog 状态：
    - `regions = 5`
    - `entries = 16`
    - `distribution_tier = regional_sharded_manifest_catalog`
    - `coverage_scope = focus_regions_only`
  - 目前 focus regions（Japan / Europe / East Asia / Russia / North America）都已有正式 shard manifest，可进入后续 runtime catalog 消费阶段。
