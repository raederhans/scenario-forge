# Export Workbench Repair 2026-04-15

## Plan
1. Sync local main to origin/main and confirm current breakage baseline.
2. Unify exportWorkbenchUi schema and remove duplicate state definitions.
3. Remove duplicated export DOM and toolbar merge artifacts.
4. Consolidate export execution flow for composite/per-layer/bake-pack semantics.
5. Run static checks and targeted verification, then record lessons learned if needed.

## Progress
- [x] Step 1: Synced local main to origin/main and confirmed export merge artifacts remain in main.
- [x] Step 2: Unify exportWorkbenchUi schema and remove duplicate state definitions.
- [x] Step 3: Remove duplicated export DOM and toolbar merge artifacts.
- [x] Step 4: Consolidate export execution flow for composite/per-layer/bake-pack semantics.
- [x] Step 5: Run static checks and targeted verification, then record lessons learned if needed.
